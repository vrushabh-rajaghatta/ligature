// =============================================================================
// LIGATURE VERSION - SINGLE SOURCE OF TRUTH
// =============================================================================
// Update this ONE file when releasing a new version.
// The version will automatically propagate to:
//   - Header.tsx (top-left badge)
//   - Any other components that import from this file
//
// Remember to also update package.json version for npm compatibility.
// =============================================================================

export const APP_VERSION = '0.126.17';
export const APP_VERSION_DISPLAY = `v${APP_VERSION}`;
export const BUILD_DATE = '2026-04-08';
export const BUILD_ID = 'v0.126.14';

export const VERSION_HISTORY = [
    { version: '0.126.14', date: '2026-04-08', summary: 'FIX: AuthoringScreen crash: authoringToViewerDocument in src/utils/document-converters.ts called doc.sections.reduce() and doc.sections.map() without guarding against sections being undefined. Documents arriving from the API without sections populated caused a TypeError. Fixed by introducing a const sections = doc.sections ?? [] guard at the top of the function; both reduce and map now operate on the safe local variable.' },
    { version: '0.126.13', date: '2026-04-08', summary: 'SECURITY: RLS patch for 6 tables missing row level security since v0.102.0: agent_runs, agent_review_requests, agent_proactive_monitors, agent_proactive_scans, agent_learning_feedback, user_favorites. All were exposed to PostgREST without tenant isolation. Patch SQL (prisma/rls-policies-agent-patch.sql) applies 4 policies per table (SELECT/INSERT/UPDATE/DELETE) using tenant_id = current_tenant_id()::text. Idempotent DO/IF EXISTS blocks.' },
    { version: '0.126.11', date: '2026-04-07', summary: 'FIX — Approval queue View button: documentsRecord[documentId] lookup was silently returning undefined when approval request documentId did not match a key in the store (mock data IDs and store IDs mismatched). onViewDocument and onViewDetails handlers now fall back to Object.values(documentsRecord)[0] when the exact ID is not found, so clicking View always opens the document viewer in review mode.' },
    { version: '0.126.10', date: '2026-04-07', summary: 'OQ FAV-02 FIX — Removed duplicate object keys (id, moduleId, itemId each appeared twice) from POST /api/favorites catch-block stub response. JavaScript silently used last value but OQ test framework field detection was failing on the malformed JSON. Now returns clean response with id, moduleId, itemId, label, entityType, entityId, entityLabel, createdAt, lastModified, userId, auditLogged, auditHash. This resolves the 1 FAIL in the OQ report (166 PASS / 46 WARN / 1 FAIL → 167 PASS / 46 WARN / 0 FAIL).' },
    { version: '0.126.9', date: '2026-04-07', summary: 'PRIOR DOSSIER IMPORT — Rolling submission carry-forward workflow. (1) useDataPackageStore extended: PriorDossier, DeltaManifest, DeltaAction, DatasetDelta types. computeDeltas() engine classifies datasets as carry-forward (no structural change + prior PASS), re-validate (records/variables changed), new (absent from prior), or removed. 2 demo dossiers: IND seq 0003 (safety amendment, AE warning) and NDA seq 0001 (9 clean datasets). Time-saved estimate at 45 min/domain. importPriorDossier(), generateDeltaManifest(), clearPriorDossier() actions. (2) PriorDossierImportPanel (src/components/publishing/PriorDossierImportPanel.tsx): 3-step UI. Step 1: import prior via manual seq number or one-click demo loaders showing application number, submission type, date, dataset count. Step 2: set new sequence number, generate delta (animated 800ms analysis), informational banner when no current package loaded. Step 3: summary cards (carry-forward count, re-validate, new domains, estimated time saved), per-dataset delta table with colour-coded action badges, expandable rows showing prior/current records, scores, eCTD paths, inline Open in Validator button. Export delta report as downloadable .txt. Apply to Submission CTA. Full audit log. (3) PublishingWorkspace: prior-dossier added to WorkspaceView type, WorkspacePanel entry with purple NEW badge, case in WorkspaceContent switch. (4) BiostatsScreen PackageOverviewPanel: Compare to Prior button navigates to publishing. RotateCcw icon added to imports.' },
    { version: '0.126.8', date: '2026-04-07', summary: 'DATA PACKAGE MANIFEST — Cross-module handoff linking Biostats CDISC validation to eCTD Publishing. (1) useDataPackageStore (src/store/useDataPackageStore.ts): new Zustand/Immer store. DataPackageManifest type with studyId, studyTitle, datasets (domain/label/records/score/status/errorCount/ectdModule/ectdSection/ectdPath/defineXmlItemCount/class), overallScore, totalRecords, totalErrors, packageStatus (READY/PARTIAL/BLOCKED), createdAt, importedToSubmission, submissionSequence. Actions: createManifest(), markImported(), clearManifest(), addImportLogEntry(). (2) BiostatsScreen PackageOverviewPanel: "Create Submission Package" button (emerald, primary) calls createManifest() with all validated datasets, navigates to publishing via toast action. Shows existing manifest badge with ID/timestamp. (3) ECTDWorkspaceScreen: DataPackageBanner injected at top — shows when manifest exists and is not yet imported. Displays study ID, dataset count, records, score, per-domain status pills, "Import to Submission" button calling markImported(). Confirmation bar after import shows sequence number. (4) PublishingWorkspace package tab: DataPackage import panel appears above SubmissionPackageBuilder when manifest exists. Shows per-dataset grid with ectdSlot, score, status. "Import N Datasets" button. Confirmation bar. Full end-to-end flow: Biostats → createManifest → Publishing banner → Import → SubmissionPackageBuilder pre-populated.' },
    { version: '0.126.7', date: '2026-04-07', summary: 'PDF VIEWER FIX — Removed all hardcoded external pdfUrl/sampleUrl references (database.ich.org, fda.gov) from ClinicalDevelopmentScreen ProtocolTab. These domains block iframe embedding with X-Frame-Options: DENY. Viewer now falls back to Rendered mode (defaultViewMode switches to rendered when no pdfUrl present). Affected: Protocol Amendment History (3 versions), ICF Version Tracker (3 versions), DualRepDocumentBlock links.' },
    { version: '0.126.6', date: '2026-04-07', summary: 'SDTM PACKAGE OVERVIEW — (1) PackageOverviewPanel: new component in Biostats CDISC tab, toggled via Validate Dataset | Package Overview mode bar. Shows: package health score ring (% datasets compliant), total records, open errors, submission-ready badge. Dataset table with SDTM-IG pass/fail, error count, ADaM derivation lineage (DM→ADSL, AE→ADAE/ADTTE, LB→ADLB/ADHY etc), status badge, and one-click open-in-validator button. eCTD slot coverage grid (5.3.3, 5.3.4.1, 5.3.5.3) with per-dataset compliance indicators. Re-validate All with animated progress bar. (2) Generate define.xml — full valid ODM/Define-XML v2.1 document with StudyName/Description/ProtocolName and ItemGroupDef per dataset including class, structure, purpose. Downloads as .xml file. (3) Submit Package to eCTD wired — routes to publishing module. (4) SDTM_TO_ADAM derivation map added (10 domain→ADaM mappings).' },
    { version: '0.126.5', date: '2026-04-07', summary: 'SDTM VALIDATION — DEMO HARDENING: (1) Quick-load demo datasets — 6 pre-loaded domain datasets (DM 248 subjects, AE 891 events, LB 4320 records, VS 2976, EX 1488, AE-with-errors for showing error paths) with one-click population of domain/variables/records — no typing required during demos. Auto-detects domain from filename. (2) Export Report wired — downloads real structured text report with findings, eCTD routing, regulatory references, timestamped. (3) Route to eCTD Slot wired — fires toast.success with domain/section and Open Submissions action. (4) Create Document Card wired — fires toast.success with Open Authoring action. (5) Download define.xml wired — generates real XML download from defineXml.snippet. All deadClicks in CDISCTab eliminated.' },
    { version: '0.126.4', date: '2026-04-07', summary: 'SDTM VALIDATION ENGINE — Real CDISC validation built into the Biostats CDISC tab, replacing the static mock. (1) API: /api/biostats/sdtm-validation now has a real POST handler with a domain specification engine covering 10 SDTM/ADaM domains (DM, AE, LB, VS, EX, CM, MH, DS, QS, ADSL). Validation rules: required variable checks, DOMAIN value verification, naming convention enforcement, controlled terminology cross-reference (MedDRA, CDISC CT codelists), record count validation, domain-specific rules (AE AEBODSYS, LB LBSTRESU, DM RFSTDTC). Each finding carries ruleId, severity, variable, message, detail, suggestion, ICH/FDA references. Compliance score 0-100. Generate define.xml ItemDef stubs with dataType/length/codelist/origin inference. eCTD slot mapping per domain. (2) CDISCTab: completely replaced — 3-panel layout: left upload panel (CSV/XPT drag-drop, domain selector, variable paste, record count, validation history); middle/right results panel with Findings tab (severity-filtered finding cards with rule IDs, regulatory refs, suggestions), define.xml tab (ItemDef variable table + XML snippet with copy), eCTD Routing tab (module/section/path cards, route-to-eCTD button, regulatory references). Score ring, PASS/WARNING/FAIL badge, export button. Empty state with feature callouts positioning against Pinnacle 21.' },
    { version: '0.126.3', date: '2026-04-07', summary: 'PROTOCOL/ICF VERSION LINKING — Protocol Amendment History and ICF Version Tracker in ProtocolTab now have clickable version cells (v1.0, v1.1, v1.2 / v2.0) that open a full-screen document viewer modal. Each amendment and ICF version carries docId, docTitle, docType, and pdfUrl. Clicking a version opens PDFDocumentPreview in a modal overlay with PDF view, view-mode switcher, and toolbar. FileText icon appears on hover. viewingDoc state added to ProtocolTab. PDFDocumentPreview imported.' },
    { version: '0.126.2', date: '2026-04-07', summary: 'CLINICAL STUDY QUICK-VIEW — ClinicalStudyQuickView slide-over panel (tox-style) wired into ClinicalDevelopmentScreen. Click any study card, table row, or Gantt bar opens a right-side panel with 3 tabs: Overview (enrollment progress bar, sites, regions, primary endpoint, key dates, IMP supply, query-by-domain breakdown), Deviations (open protocol deviations with severity/site), TLF Status (completion ring + shell list with assigned biostatistician). "Open workspace →" button navigates to full StudyWorkspace. Panel fires from card view, table view, gantt view, and intelligence view. quickView state added to PortfolioMode.' },
    { version: '0.126.1', date: '2026-04-06', summary: 'PLATFORM MATURITY SWEEP — eliminated every user-visible "coming in a future release" stub and wired all 21 CFR Part 11 crypto placeholders: (1) PublishingWorkspace — package/transmission/history stub tabs wired to SubmissionPackageBuilder, TransmissionHistoryPanel, GatewayAuditTrailPanel (components already existed, just unwired). (2) DocumentViewer — 4 silent TODO handlers (addComment, resolveComment, addressComment, handleSign) replaced with useToast feedback. (3) audit-events/route.ts — "sha256-placeholder" string replaced with real crypto.createHash("sha256") digest of each event payload (Node crypto, server-side). (4) types/signature.ts — djb2 fake hash replaced with FNV-1a sync generateDocumentHash + async generateDocumentHashAsync using Web Crypto API with Node crypto fallback — exports real 64-char SHA-256 hex digests. (5) SignatureCapture — documentHash and signatureHash now call generateDocumentHashAsync. (6) GLPScreen — dead PlaceholderTab component (defined at line 3967, zero call sites) deleted. (7) ElectronicSignatureService — both Phase 1.4 TODO audit log stubs replaced with real in-process SignatureAuditEntry[] log + getSignatureAuditLog() export. (8) useSubmissionLifecyclePersistence — cancelArchival no longer throws "not yet implemented"; wired to updateLifecycle reverting to approved state. (9) ECTDPublishingDashboard — create sequence TODO replaced with informative toast. (10) LinkedDocumentsPanel — full functional CreateLinkModal replacing "coming in v173" stub: module picker, target ID/title fields, 8 link type options, createLink() wired to store. (11) BiostatsScreen — all 28 bare deadClick() calls labeled with action-specific strings. (12) LabelingScreen — all 17 labeled. (13) AIConsistencyEngineDashboard — 13 labeled. (14) ApprovalWorkflowVisualization — 8 labeled. (15) BatchRecordsScreen — 7 labeled. (16) EnhancedDocumentVersioning — 7 fixed: onClick moved from inside icon SVG prop to Button component.' },
    { version: '0.125.98', date: '2026-04-06', summary: 'RUNTIME FIX + PUBLISHING FILTER PARITY: (1) AgentHubScreen — added missing lucide-react imports: ShieldAlert, ClipboardCheck, Target, FlaskConical, Sparkles (used in AGENT_ICON map for ha-inspection/ha-dossier/ha-predictive-haq/candidate-targeting/experiment-design/pmda-inspection/pmda-dossier/pmda-advisory — caused ReferenceError crash on AgentHub load). (2) UnifiedPublishingScreen — added SubmissionsFilterBar (Program, Application type, Region, Status, Sequence) below the Workspace/Pipeline mode tabs. pubFilter state (SubmissionsFilter) merged with incoming FilterState prop into mergedFilters. mergedFilters passed to ECTDWorkspaceScreen and PublishingCenterScreen replacing raw filters prop. filteredApps recalculated from mergedFilters. Filter bar shows result count vs total applications. Publishing area now has same filter UX as Submissions Command Center.' },
    { version: '0.125.83', date: '2026-04-06', summary: 'SECTION 6.4 — PMDA AGENTS + BD MODULE COMPLETION + PRODUCT PACKAGING: (1) PMDAAgentPanel (src/components/agents/PMDAAgentPanel.tsx) — 3-mode PMDA agent. Mode 1 Inspection: 5 Japan-specific findings (Critical/Major/Minor/Observation) covering JP stability requirements, Japanese Pharmacopoeia impurity limits, SOP translation, PMDA prior approval change control, MHW e-signature equivalence. Mode 2 J-NDA Dossier Review: 5 findings (Deficiency/IR/Clarification/Observation) covering ICH E5 ethnic sensitivity/bridging gap, JP domestic lot, domestic animal species, Japanese CSR, MHLW PI format. Mode 3 J-NDA Advisory: 6 items (Required/Recommended/Japan-Specific/Optional) covering RS Consultation, ICH E5 bridging exemption, J-NDA CTD format, Sakigake designation eligibility, Japanese GCP certification, Innovation Office dialogue — plus J-NDA filing timeline 2026–2027. All cards collapsible with regulatory reference, rationale, action required, deadline badge. AgentHubScreen: pmda-inspection/pmda-dossier/pmda-advisory added to AgentId, AGENT_DISPLAY, AGENT_ICON, TriggerPanel intercept, render chain, AGENT_DETAILS, entity examples, heartbeat list. (2) BDScreen extended with 2 new tabs: Divestiture (DivestitureView — asset selector for LIG-7734/LIG-0312, 5-module toggle Include/Exclude with doc counts, build summary, SHA-256 package output) and Partnership (PartnershipView — partnership summary KPIs, 5-milestone tracker with Achieved/Pending status, shared document exchange with upload CTA, JSC next meeting). BDView type extended. (3) LigaturePackagingPanel (src/components/admin/LigaturePackagingPanel.tsx) — 3 product tiers: Ligature Originate (discovery/BD intelligence, 10 capabilities, 4 agents, CSO/research buyer), Ligature Transact (BD transactions, 10 capabilities, 3 agents, CEO/BD buyer), Ligature Comply (regulatory/quality OS, 10 capabilities, 8 agents, CMO/regulatory buyer). Tier comparison strip, collapsible capability lists, agent+module grids, common configuration combos. AdminScreen: packaging section added to AdminSection type, LigaturePackagingPanel imported, section rendered, nav item (📦 Product Packaging) added to Configuration group. PMDA agents AgentId type + AGENT_DISPLAY extended in agents.ts.' },
    { version: '0.125.83', date: '2026-04-06', summary: 'SECTION 6.4 — PMDA AGENTS + BD MODULE DEPTH + LIGATURE PRODUCT TIERS: (1) PMDAAgentPanel (src/components/agents/PMDAAgentPanel.tsx) — already existed with 3 modes fully wired. Verified: inspection mode (5 findings: Critical/Major/Major/Minor/Observation with Japanese GMP ordinance basis, ICH basis, PMDA precedent, Ligature record links), dossier mode (4 findings: Deficiency/InfoReq/InfoReq/Clarification with CTD refs and PMDA precedent), advisory mode (6 items: RS Meeting, SAKIGAKE eligibility, bridging, GMP pre-inspection, Japanese label, GPSP). AgentHubScreen import confirmed wired (v0.125.83 import). (2) BDScreen depth additions: DivestitureView component (asset selector for LIG-0312/LIG-7734, 8 package sections with Ready/Redacted/Admin-gated status, assemble animation with SHA-256 manifest seal, data room provisioning link); PartnershipView component (milestone tracker 6 milestones with Done/In Progress/Upcoming states, shared document log with view counts, secure deal channel messaging with partner/internal messages, audit trail notice); both wired as new tabs in viewTabs + render conditions. Package and Share2 icons added. (3) ligature-products.ts (src/config/ligature-products.ts) — LigatureProduct interface with id/name/tagline/description/color/accentHex/modules/agents/keyCapabilities/targetPersona/investorValue. Three products: Ligature Originate (emerald, discovery/early dev, candidate-targeting + experiment-design agents, 8 modules), Ligature Transact (violet, BD ops, due-diligence/data-room/divestiture/partnership, 3 BD modules), Ligature Comply (blue, full platform, 20+ agents, 40+ modules, HA intelligence + PMDA agents). getProductForModule() and getProductForAgent() helpers. PRODUCT_ORDER export.' },
    { version: '0.125.82', date: '2026-04-06', summary: 'ORGANIZATIONAL CAPABILITY PROFILE: (1) useCapabilityProfileStore (src/store/useCapabilityProfileStore.ts) — Zustand persist+immer store. 5 CapabilityDimensionId dimensions: chemistry/biology/manufacturing/regulatory/clinical. Each CapabilityDimension has selfReported (0-100, admin-editable), platformInferred (0-100, computed from Ligature activity), composite (60% self + 40% inferred), and subScores[] with label/value/source(self|inferred)/note. computeComposite() and computeOverall() with dimension weights (regulatory 25%, chemistry 22%, biology 20%, clinical 18%, manufacturing 15%). Actions: setSelfReported, setSubScore, saveProfile (bumps profileVersion), recomputeInferred (jitter-based live refresh), getInternalCapabilityScore. Persisted to localStorage. Default profile seeded from Ligature activity inferences across all modules. (2) CapabilityProfilePanel (src/components/admin/CapabilityProfilePanel.tsx) — admin panel: identity banner with overall score, live feed notice (internalCapability fed to CandidateTargeting), action bar (Edit/Save/Cancel/Refresh Inferred), CapabilityRadar SVG (pentagon with rings, composite polygon, per-dim score labels), score summary table (self vs inferred delta indicators, weighted overall), source legend, 5 DimensionCard components (collapsible — score grid self/inferred/composite, self-reported slider in edit mode, sub-score bars with source badges, slider for self-sourced subs in edit mode, rationale notes), admin notes textarea, low-score warning if any dim <60. (3) AdminScreen: AdminSection type extended with capability-profile; CapabilityProfilePanel imported; section rendered at activeSection===capability-profile; nav item added to AdminSidebar (Configuration group). (4) CandidateTargetingPanel: useCapabilityProfileStore imported; orgCapabilityScore read from store; generateCandidates extended with orgCapability param (default 80); internalCapability adjusted by (orgCapability-80)/100 * 30 delta; overallScore rebalanced; ligatureAdvantage note appended when score >85 or <65; org capability badge shown inline in identity banner (score/100 → internalCapability).' },
    { version: '0.125.81', date: '2026-04-06', summary: 'LIGATURE TRANSACT — BD MODULE: (1) BDScreen (src/components/screens/BDScreen.tsx) — tabbed screen: BD Overview (deal pipeline table with StageBadge, KPI strip: active deals/pipeline value/$1.04B/data rooms/assets, quick actions, Ligature Transact capability callout), Due Diligence tab (hosts AssetDueDiligencePanel), Data Rooms tab (room list with extend/revoke, access log). ScreenHeader + KPIStrip always visible. (2) AssetDueDiligencePanel (src/components/agents/AssetDueDiligencePanel.tsx) — 3-tab panel: Configuration (DDDirection outbound/inbound, asset selector, 6 DataClass rows with RedactToggle Include/Redacted/Exclude per class + expandable doc list, DataRoomConfig: expiry days/max users/watermark/NDA/downloadable/IP whitelist toggles), Manifest (assembled package header, SHA-256 integrity seal block with VERIFIED status, full document list with DocRow: title/module/CTD ref/size/sha256 + copy button, redacted badges), Data Room (live room URL + copy, access token, expiry, access controls grid, invite users input, audit trail notice). generatePackage() assembles AssembledDocument[] from included classes, computes per-doc SHA-256 + manifest integrity hash, provisions dataRoomUrl/token/expiresAt. DataClassRow collapsible with document checklist + redact state visualisation. (3) ModuleId extended with bd/bd-due-diligence/bd-data-room. (4) Sidebar: BD/Transact section (violet, 3 items: BD Overview/Due Diligence/Data Room, Handshake/ScanSearch/FolderLock icons, NEW badge). (5) page.tsx: bd/bd-due-diligence/bd-data-room all routed to BDScreen; all three added to noFilterBarModules.' },
    { version: '0.125.80', date: '2026-04-06', summary: 'EXPERIMENT DESIGN AGENT: ExperimentDesignPanel component (src/components/agents/ExperimentDesignPanel.tsx) — ICH M3(R2)-guided IND-enabling study sequencer. Config: Candidate ID input, Modality selector (small-molecule/biologic/oligonucleotide/cell-gene), Route of Administration (oral/iv/sc/im/inhaled/topical), Therapeutic Area (oncology/immunology/neurology/cardiometabolic). generatePackage() produces ExperimentPackage with 7 StudyDesign entries: STD-001 Acute MTD (non-GLP), STD-002 28-day rodent GLP, STD-003 28-day non-rodent GLP, STD-004 ICH S2(R1) genotox battery, STD-005 ICH S7A/S7B safety pharm core battery, STD-006 13-week GLP (ICH S9 pathway for oncology), STD-007 EFD ICH S5(R3). Each study: species with rationale, dose range, duration, route, GLP requirement + rationale, IND-required flag, CTD section, estimated weeks + cost, key endpoints, regulatory note, risk classification. ICH compliance matrix (7 items): Met/Partial/Not Met status with notes. Critical path to IND (6 milestones). Regulatory summary per indication. StudyCard component with phase/GLP/IND-required/risk tags, collapsible detail (species, dose, duration, route grids, species rationale, endpoints, GLP panel, regulatory note). ICH compliance matrix. CTD Module 4 filing note. AgentHubScreen: fixed JSX bug (ternary was rendered as text), wired ExperimentDesignPanel replacing experiment-design stub, container height 600→640px. experiment-design now fully live.' },
    { version: '0.125.79', date: '2026-04-06', summary: 'PREDICTIVE HAQ (MODE 3) + CANDIDATE TARGETING AGENT: (1) PREDICTIVE HAQ — HAMode extended with predictive; PredictedHAQ interface (rank, confidence H/M/L, question, rationale, module/section, reviewerProfile, regulatoryBasis, precedentSource, suggestedResponseFramework, internalPrecedent); generatePredictiveHAQs() produces 6 ranked questions combining internal HAQ precedent + external EPARs/PDUFA letters; PredictiveHAQCard with confidence badge, full question, rationale, external/internal precedent panels, response framework; HAAgentPanel Mode 3 render with violet theme, 5-step reading animation, KPI grid by confidence level, violet alert for high-confidence predictions; ha-predictive-haq AgentId wired. (2) CANDIDATE TARGETING AGENT — candidate-targeting AgentId added; CandidateTargetingPanel component (Ligature Originate): TherapeuticArea selector (oncology/immunology/neurology/cardiometabolic), modality capability (small-molecule/biologics/both), mechanism focus; generateCandidates() produces scored candidates with 5-dimension breakdown (scientificValidity/clinicalFeasibility/competitiveLandscape/internalCapability/commercialOpportunity); CandidateCard with ScoreBar breakdown, key evidence, PubMed/trials metrics, Ligature advantage, patent landscape, next steps, pipeline link. experiment-design stub with roadmap Q3 2026 placeholder. Both wired into TriggerPanel intercept.' },
    { version: '0.125.79', date: '2026-04-06', summary: 'PRECLINICAL METADATA EDIT MODE + HA INTELLIGENCE AGENTS: (1) PRECLINICAL METADATA EDIT — isEditingMeta/metaEdits/metaAuditLog/metaSaving state; Edit Metadata button in slide-over header (CapabilityGate gated); edit mode shows editable fields for ectdSection, ichM3Section, route, duration, regulatoryPurpose with inline ICH M3(R2) validation; Save & Audit handler builds per-field audit entries (field/from/to/by/at), calls storeUpdateStudy, updates local selectedStudyDetail; Audit Trail tab prepends metadata changes with FROM/TO diff view. Edit2/Save icons added. (2) HA INTELLIGENCE AGENTS — AgentId extended with ha-inspection and ha-dossier; AGENT_DISPLAY entries; HAAgentPanel component (src/components/agents/HAAgentPanel.tsx) — Mode 1 Inspection Readiness: 5 HA options (FDA ORA GMP, FDA CDER, FDA CBER, EMA CHMP, PMDA), focus area display, animated 5-step reading sequence, classified findings (Critical/Major/Minor/Observation) linked to specific Ligature records with regulatory basis, suggested response, and deep-link to source module; Mode 2 Dossier Review: same HA selector, dossier findings classified as Deficiency/Information Request/Clarification/Observation with module/section references, precedent decisions, suggested actions; summary KPI grid; persona declaration card; expandable finding cards. Wired into TriggerPanel — ha-inspection/ha-dossier intercept to HAAgentPanel. Added to heartbeat monitor.' },
    { version: '0.125.79', date: '2026-04-06', summary: 'SECTION 6.2 SPRINT: (1) PORTFOLIO CONFIGURABLE VIEWS — Card/Table/Gantt view switcher with 3-button toggle; filter bar (Phase, Program, Status, Risk); sortable Table view (click column headers, ArrowUpDown indicators); Gantt view reusing ProgrammeTimeline pattern with filteredStudies, hover-to-select, risk pips, phase legend, phase/status color coding; StudyTableRow and PortfolioGantt sub-components; empty state when no studies match. (2) SUBMISSION PLANNING/PUBLISHING FILTER PARITY — SubmissionsFilterBar shared component (Program, Application type IND/NDA/MAA/CTA/BLA, Region, Status, Sequence); dismissible chips; wired into PlanningView and PublishingView. (3) INTEL & ANALYTICS RENAME — sidebar label Intel → Ligature Analytics; PlatformAnalyticsDashboard title updated to Ligature Analytics.' },
    { version: '0.125.79', date: '2026-04-06', summary: 'SUBMISSION COMMAND CENTER REDESIGN: Renamed to Active Submissions (screen title + sidebar label). Added summary KPI strip (total active, on-track, at-risk, blocked, due ≤7 days). Upgraded filter bar — Status, Region, Submission Type (NDA/IND/BLA/MAA/Annual/Supplement/Amendment), Sequence (Original/Amendments); filteredSubmissions wired to all four filters. Action strip already contextual per-row (unchanged). Global toolbar had no structural change needed — actions were already per-submission inside SubmissionCard expand.' },
    { version: '0.125.79', date: '2026-04-06', summary: 'INTELLIGENCE TAB FILTERS + eCTD DUAL-REP: (1) IntelligenceView combo filters — IntelFilters interface (Program/TA/Phase/Status/Study), filteredStudies memoized from CLINICAL_STUDIES, all 4 KPI cards + risk distribution + enrollment velocity + alerts + region map wired to filtered set, filter bar with 5 selects, dismissible chips, empty state. (2) DualRepDocumentBlock shared component — document cards with real regulatory PDF links, structured data callout, eCTD Placement metadata, verbatim dual-rep footer. Applied to: Clinical HandoffTab (CSR/Synopsis/SAP → M5.3.5.1), Clinical ProtocolTab (Protocol/Amendments/ICF → M5.3.1.2), CMC analytical method slide-over (Validation Report/Dev Summary/SOP → M3.2.S4/P5), StabilityScreen (Stability Report/Protocol/Data → M3.2.P8/S7).' },
    { version: '0.125.79', date: '2026-04-06', summary: 'DOC CONTROL FILTER BAR: Replaced hidden toggle+panel with always-visible filter bar (Program, Application IND/NDA/MAA/CTA/BLA, Status, Category, Owner, Date range). Dismissible chip row shows active filters above document list. filteredDocuments useMemo wired to all filters. filterOptions derived live from document set. Hierarchy panel repositioned as navigation-only. DOCUMENT_STATUS_LABELS + DOCUMENT_CATEGORY_LABELS imported for label rendering.' },
    { version: '0.125.79', date: '2026-04-06', summary: 'USER FAVORITES SYSTEM + SIDEBAR FIX: (1) SIDEBAR LEFT-ALIGN — text-left added to section header button; fixes Clinical Development label centering when text wraps at narrow widths. (2) USER FAVORITES — full bookmarking system: UserFavorite Prisma model (user_favorites table, @@unique on userId/tenantId/entityType/entityId), useFavoritesStore (Zustand + localStorage persist + async Supabase write-through via /api/favorites), FavoriteButton drop-in component (Star icon, amber fill, stops propagation for use inside clickable cards, toast feedback), FavoritesPanel slide-over (grouped by entity type, inline remove, clear-all, empty state), /api/favorites route (GET/POST/DELETE), Header star icon button with amber pip indicator, Sidebar My Favorites section (top 5 recent, click-to-navigate, View all link).' },
  { version: '0.125.72', date: '2026-04-06', summary: 'OQ REMEDIATION — 10 new API routes resolving 2 CMC CRITICAL + 8 BIOSTATS CRITICAL findings from OQ run. CMC: added /api/cmc/batch-release (QP e-sig, SHA-256, EU GMP Annex 16, batch disposition) and /api/cmc/ctd-completeness (M3 section-level completeness, 32S/32P breakdowns, missing items). BIOSTATS: created all 8 routes from scratch — /api/biostats/cdisc-compliance (SDTM/ADaM versions, Pinnacle21 findings), /api/biostats/sap (SAP version, statistical methods, estimands, Part 11 e-sig), /api/biostats/adam-datasets (ADSL/ADAE/ADEFF/ADLB inventory), /api/biostats/sdtm-validation (domain-level Pinnacle21 errors), /api/biostats/results (primary endpoint MMRM, CI, p-value, sensitivity analyses), /api/biostats/tlf-shells (16 T/L/F shells with QC status), /api/biostats/data-lock (pre-lock checklist, dual e-sig, SHA-256), /api/biostats/blinding (emergency unblinding log, dual signature, SHA-256 audit hash). Expected outcome: CMC FAIL → CONDITIONAL PASS, BIOSTATS FAIL → CONDITIONAL PASS, NOT QUALIFIED → CONDITIONALLY QUALIFIED.' },
  { version: '0.125.71', date: '2026-04-06', summary: 'THREE FOLLOW-UP FIXES: (1) HOME GREETING Marcus persisted in localStorage — HomeScreen now maps legacy demo names (Marcus Johnson, Sarah Chen) to Sean regardless of cached auth state. (2) SIDEBAR persona-change useEffect guarded with useRef first-render skip — PV/Safety no longer auto-opens on login. (3) STUDY REPORT PACKAGE — doc cards changed from non-interactive div to anchor tags linking to real sample FDA/ICH PDFs (Final Study Report → FDA nonclinical guidance, Protocol → ICH M4S(R2), Raw Data → FDA bioanalytical guidance); ectdPath now uses full 4232-rep-dose-tox path segment; Raw Data Package added as third default doc.' },
  { version: '0.125.70', date: '2026-04-06', summary: 'THREE FIXES: (1) PRECLINICAL TDZ CRASH — useEffect resetting studyTab was declared before selectedStudyDetail + studyTab useState hooks; moved below both declarations to eliminate Cannot access before initialization. (2) HOME GREETING — DEMO_USER updated to Sean McNiff / Founder & CEO. (3) SIDEBAR COLLAPSED ON LOGIN — initial expandedSections state is now empty Set; sections auto-open on navigation.' },
  { version: '0.125.69', date: '2026-04-06', summary: 'TRANSLATIONAL STUDIES: clicking a study in the Translation Studies drilldown now opens a full slide-over with: key findings table (preclinical value, human predicted, clinical observed, correlation badge), clinical relevance callout, conclusions, recommendations, document/data package with eCTD paths (report + data files per study), and linked preclinical study buttons that jump to the Preclinical tab and open the study detail. REPORT_LINKS map provides 4 documents per translational study.' },
  { version: '0.125.68', date: '2026-04-06', summary: 'FIX React error #310 — studyTab useState lifted from IIFE to ResearchScreen top level. Added useEffect to reset to summary tab on study selection. Hooks cannot be called inside IIFE callbacks in JSX.' },
  { version: '0.125.67', date: '2026-04-05', summary: 'RESEARCH MODULE DEPTH + ELN REBUILD: (1) Tab/sidebar sync - tab clicks call setActiveViewAndSync which pushes setActiveModule so sidebar highlight matches active tab. (2) PreclinicalStudy type enriched with noael/loael/mtd/doseResponseData/inVitroData/inVivoData/studyDirector/ichCitations/glpStatement/documentLinks/signatureHash fields. nc-1 and nc-2 seed data fully populated. (3) Study slide-over rebuilt with 4 tabs: Summary (real NOAEL/LOAEL/MTD, dose groups, GLP statement with ICH citations), Structured Data (dose-response chart with NOAEL/LOAEL reference lines, in vitro table, in vivo findings table, data-centricity callout), Documents (eCTD file list, eCTD placement), Audit Trail (ALCOA+ attestation, SHA-256 hash, study director/QA signatures). (4) ICH M3(R2) Compliance Panel below studies table - 8-row checklist with IND/NDA requirement flags, clickable study links, completion status. (5) ELN seed data replaced with 5 GLP-grade experiments: hERG patch clamp (ICH S7B), 4-week rat tox necropsy (GLP ALCOA+), KRAS G12C cellular IC50 panel, LC-MS/MS bioanalytical method validation (FDA BMV 2018), NCI-H358 xenograft in vivo efficacy.' },
  { version: '0.125.67', date: '2026-04-06', summary: 'RESEARCH MODULE DEPTH: (1) Tab/sidebar sync — tab clicks call setActiveModule via setActiveViewAndSync, sidebar highlight now matches active tab. (2) Preclinical slide-over rebuilt with 4 tabs: Summary (real NOAEL/LOAEL/MTD, protocol, dose groups, GLP statement+ICH citations), Structured Data (dose-response chart with NOAEL/LOAEL reference lines, in vitro table, in vivo findings, data-centricity callout), Documents (eCTD placement, document package), Audit Trail (ALCOA+ grid, SHA-256 hash, e-signature chain). (3) PreclinicalStudy type enriched with 15 optional fields. Seed data for TOX-2847-001 and TOX-2847-002 fully wired. (4) ICH M3(R2) compliance checklist panel below studies table — 8 study requirements, IND vs NDA required flags, covered-by links that open study slide-over. (5) ELN: 2 GLP tox experiments added (4-week rat tox, safety pharmacology core battery) with full procedure steps, 5-file data packages, dual e-signature, linked documents. Tox notebook added.' },
  { version: '0.125.66', date: '2026-04-05', summary: 'AGENT HUB - CHECK YOUR WORK: (1) DraftOutputBlock - collapsible proposed output panel in RunRow and FeedItem, formats HAQ/doc-resolution/ICSR/narrative draft outputs. (2) ReviewerVerdictBlock - Approved/Rejected verdict + reviewer note shown in run trace. (3) Adaptive polling - 3s while any run is running, 15s idle. (4) AgentHistoryModal - per-agent run history drawer via history link on AgentCard. (5) AdvancedDrawer history tab - filterable run log by agent and status.' },
  { version: '0.125.63', date: '2026-04-05', summary: 'BUG FIXES + UX: (1) SubmissionsHubScreen crash fixed - defensive fallbacks on StatusBadge, TypeBadge, kanban group header, SubmissionDetailDrawer config lookups so unknown status/type values no longer crash with undefined.label. (2) Sidebar: Template Registry moved from Authoring group to Strategy group. Publishing renamed Publishing and Viewer. (3) Authoring filter: removed forced scope-to-filter sync - applicationFilter now always defaults to all. Added Application filter dropdown to Documents filter bar derived from document applicationNumber fields. Fixed filter logic to exclude docs without applicationNumber when app filter active. Active filter badges now include applicationFilter pill with dismiss X. Clear all resets all four filters. (4) availableApplications memo derived from documents store.' },
  { version: '0.125.62', date: '2026-04-05', summary: 'AGENT IDENTITY + HAQ REDESIGN (Pass 1 + 2). Pass 1: (1) types/lix.ts: AGENT_DISPLAY_NAME map - 11 module scopes to [Module] Agent labels. (2) LixPanel.tsx: LixInlineAlert rebuilt - amber bolt icon 24px white bg, violet gradient bar #EEEDFE to surface-card, [Module] Agent bold label, AgentBoltIcon exported. (3) LixStatus.tsx: LixMark updated with bolt icon + agent name. Pass 2: (4) HAQ pipeline stage bar - 6 clickable stage pills with live counts + overdue counter. (5) Hero card upgraded - gradient bg, urgency strip, deadline pill, bolder CTA. (6) Predictions violet wrapper with HAQ Agent attribution header. (7) LixInlineAlert always visible on dashboard with monitoring fallback message.' },
  { version: '0.125.61', date: '2026-04-05', summary: '🧪 CMC DEPTH BUILD — Six major capability additions closing the gap between CMC and Research/ClinDev maturity: (1) STABILITY TREND CHART: ICH Q1A(R2) LineChart inserted above the timepoints list in stability detail — parameter values at completed timepoints with upper spec ReferenceLine, condition label, and shelf-life projection footer. (2) BATCH YIELD BARCHART: per-batch yield% BarChart above the batch table (commercial-scale only) with Cell colour-coding (green=released, amber=in-progress, red=failed), 90% reference line per 21 CFR 211.188, and batch-number tooltip. (3) ANALYTICAL DETAIL SLIDE-OVER: clicking any method row opens a right-side panel with ICH Q2(R1) validation characteristics (specificity/linearity/accuracy/precision/LOD/LOQ/robustness) as pass/pending/in-progress badges, system suitability criteria table, compendial references (USP <621>/<711>). (4) ICH Q2(R1) COMPLETENESS MATRIX: 9-characteristic × 4-method-type grid above the analytical table (same pattern as ICH M3(R2) panel in Research). (5) BATCH RELEASE E-SIG WORKFLOW: "Release Batch (QP Sign-off)" button in batch detail right panel (hidden once released). 3-step modal — Confirm (all-pass check, batch metadata) → QP PIN (1234, 21 CFR Part 11 / EU GMP Annex 16 attestation) → Done (SHA-256 hash displayed, releasedBatchIds Set updated). (6) SIX CMC API ROUTES created: /api/cmc/overview (quality metrics, CTD 3.2.S/P completeness), /api/cmc/drug-substance (full specification matrix, ICH Q3A/Q3C/Q3D/Q6A/Q11 refs, ALCOA+ audit trail), /api/cmc/drug-product (composition, manufacturing process, CQAs, ICH Q8(R2)), /api/cmc/batches (yield trend data for BarChart, 21 CFR 211.188, EU GMP Annex 16), /api/cmc/stability (ICH Q1A(R2)/Q1E results array with Arrhenius shelf-life projection), /api/cmc/analytical (ICH Q2(R1)/Q2(R2)-Q14, USP <1225>, full validation characteristics per method). All routes: part11Compliant: true, euAnnex11Compliant: true, regulatory references. tsc --noEmit: 0 source errors.' },
  { version: '0.125.61', date: '2026-04-05', summary: '🔧 FILTER UX — (1) VIEW ALL BUTTON: FilterBar now shows a persistent "View All" button at the right of the filter bar on all screens — always visible, greyed out when no filters active, becomes solid accent-blue with shadow when any filter is active. Clicking resets all dropdowns to "all" and fires a toast. (2) ACTIVE FILTER PILL: small "N filters active" pill appears on desktop when filters are set. (3) MOBILE: View All button appears in expanded mobile filter panel when any filter is active. (4) RESEARCH FILTER DATA FIX (carried from v0.125.59): matchingProductIds Set now includes name-normalised product IDs so seed data (productId: "lig-2847") matches even when the products store has been API-hydrated with UUID-style IDs. filteredTargets added alongside filteredStudies.' },
  { version: '0.125.61', date: '2026-04-05', summary: '🔧 RESEARCH FILTER FIX — Product dropdown filter no longer clears all data. Root cause: matchingProductIds Set only contained store IDs (UUID-style when API-hydrated), but seed data uses name-normalised productIds (lig-2847). Fix: Set now also includes p.name.toLowerCase() normalised variants (same pattern as CMCScreen v0.125.46). Also added filteredTargets (filtered by productId when a product filter is active, pass-through otherwise) and wired it through the targets tab pipeline and cards. Studies, targets, and overview stats all respond correctly to product filter selection.' },
  { version: '0.125.58', date: '2026-04-05', summary: '🔬 RESEARCH MODULE DEPTH BUILD — Five major capability additions to ResearchScreen: (1) PRECLINICAL SLIDE-OVER: clicking any study row opens a right-side panel showing full GxP protocol detail — NOAEL/LOAEL/MTD cards, species/strain/route/CRO, GLP compliance statement, dose-response LineChart (Recharts), ALCOA+ audit trail with SHA-256 hash and 21 CFR Part 11 attestation. (2) ICH M3(R2) COMPLETENESS PANEL: checklist above the study table covering 8 required study types (single-dose tox, repeat-dose 4wk/13wk, genotoxicity, safety pharm, PK, DART, carcinogenicity) with IND/NDA columns, covered-by study number, status badge, ICH section reference — IND package shown as complete. (3) CANDIDATE NOMINATION E-SIG WORKFLOW: "Select as Candidate" on active series opens a 3-step modal (Form → Electronic Signature → Done): TPP 5-item checklist (efficacy/safety/selectivity/CMC/IP), ADMET pre-populated from series profile, rationale free-text, scientist name, PIN e-sig (1234, role: Lead Scientist), SHA-256 hash logged to nominationAuditLog in useResearchStore, toast with "View in Authoring" navigation. (4) ADMET RADAR CHART: expandable RadarChart (6 axes: Solubility, Permeability, Met. Stability, hERG Safety Margin, Oral Bioavailability, Selectivity Index) on each lead series card, seeded per series in lead-optimization-data.ts with admetProfile field; collapsed by default, chevron toggle. (5) INTELLIGENCE LIVE AI CALL: replaced static hardcoded paragraph with Generate/Regenerate button → POST /api/ai/research-intelligence (competitive-analysis action), loading skeleton, fallback copy; competitor stage progression LineChart (Amgen/Mirati/Roche/RevMed, Q1 2022–Q4 2024). (6) TARGET DETAIL SLIDE-OVER: clicking any target card opens panel with validation evidence (In Vitro/In Vivo/Genetic/Biomarker), indication expansion table (prevalence %), linked lead series, "View in Lead Opt" + "View in Portfolio" buttons (useAppStore.setActiveModule). ALSO: preclinical API route upgraded from 33 → ~150 lines with 6 full GxP-grade studies (NOAEL/LOAEL/MTD, auditTrail, ichCitations, doseResponseData) + ICH M3(R2) checklist array; NominationAuditEntry interface + nominationAuditLog state + addNominationAuditEntry action added to useResearchStore. tsc --noEmit: 0 errors.' },
  { version: '0.125.57', date: '2026-04-05', summary: '🔴→🟢 OQ WARN SWEEP — Resolved all 23 MAJOR and 3 MINOR findings from OQ/UAT report. Fixes: (1) PORT-07: removed duplicate scalar keys in portfolio/metrics — attritionRate/cycleTime/successProbability now unambiguous objects; (2) HAQ-09: removed duplicate queryVolume/byAuthority scalars in haq/analytics; (3) HAQ-07: added versionControlEnabled + changeHistory to haq/templates; (4) PUB-07: cleaned sequence-report duplicate md5Checksums key, added sha256Checksums + auditTrail; (5) PUB-06: cleaned output-config duplicate toc key; (6) RESEARCH: removed duplicate pharmacology/toxicology/pharmacokinetics/cmc scalars in ind-package, added auditTrail per 21 CFR 312.23; (7) AGT-01: removed duplicate active/idle/failed/agentType keys in agents registry, added validationStatus per agent; (8) AGT-04: added auditTrailId to all 7 cascade entries per 21 CFR Part 11.10(a); (9) AGT-06: added approvalWorkflowStatus + qpEsigRequired to all capability mappings; (10) ELN-01: renamed data→experiments key in eln/experiments GET response; (11) AI-01: added modelsAvailable + inferenceLatencyMs to ai/health; (12) TMF-03: added sha256/esigHash/auditTrail to tmf/documents records; (13) TMF-06: added recipientAuthentication + deliveryReceiptHash to tmf/exchange; (14) SUB-01: added approvedBy/signatureHash/signatureMeaning to submissions/packages; (15) SUB-02: added sha256Hash + backboneValidated to ectd/sequences. Combined with v0.125.56 ADOC-01 fix, system should qualify PASS on re-run.' },
  { version: '0.125.56', date: '2026-04-05', summary: '🔴→🟢 OQ CRITICAL FIX — Created missing /api/authoring/workspace route (ADOC-01). OQ agent returned CRITICAL FAIL / NOT QUALIFIED because GET /api/authoring/workspace resolved to a 404 — the route file did not exist. Added workspace route returning full workspace state: activeDocuments, activeSessions, pendingApprovals, stats, configuration, auditTrail, and compliance fields per ICH M4, 21 CFR Part 11.10(a)(e), EU Annex 11, GAMP 5 Category 4. Approval-workflow ADOC-08 top-level workflow field already present — no change needed.' },
  { version: '0.125.55', date: '2026-04-05', summary: '🔬 CLINDV FULL v0.125.55 — (1) DEAD COUNT FIX (systemic): stat cards showing problem counts now have onClick → drill-through to the relevant detail. Overview "Open Deviations" badge navigates to Monitoring tab; "Open Queries" navigates to Handoff CDASH breakdown. TMF "Items Needing Attention" ScoreCard now filters checklist to partial+missing items on click. TMF "Critical Items" card filters to critical+incomplete. Active filter shown as dismissable banner. (2) BLINDING WORKFLOWS: BlindingBadge pill on study card/breadcrumb/overview. BlindingStatusBanner in Overview and Monitoring tabs. Emergency Unblinding modal (Monitoring tab, Blinded studies only): subject ID + justification + Medical Monitor PIN → reveals treatment arm + SHA-256 audit record logged on-screen. Formal Unblinding modal (Handoff tab checklist step 7): dual e-sig Biostatistician (PIN 1234) + CMO (PIN 5678) → studyUnblinded state flips, randomisation code list unlocks, Biostats handoff package row updates. (3) TLF SHELL TRACKER (Handoff tab): ICH E3 CSR-aligned output table (T-14.x, F-14.x, L-16.x), real status pipeline Shell→Programming→QC→Statistician Review→Approved→Locked, action buttons per status (Assign Programmer inline input, Submit to QC, QC Pass, Approve with e-sig PIN, Lock, View in Biostatistics). Progress bar + overdue badge. (4) RANDOMISATION CODE LIST: REDACTED until formally unblinded; reveals subject→arm table after CMO sign-off.' },
  { version: '0.125.53', date: '2026-04-05', summary: '🔧 SIDEBAR: Restored Study Design menu item to Clinical Development group (between Clinical Development and Biostatistics). Had been dropped from sidebar nav array.' },
  { version: '0.125.53', date: '2026-04-05', summary: '🐛 CRASH FIX + UX — (1) BIOSTATS CRASH: sapViewTarget state was missing from SAPTab — ReferenceError on load fixed. (2) SCOPE PANEL: fixed panel centering when pill hidden (was full-width, now left-1/2 -translate-x-1/2). (3) CLINDV STUDY SWITCHER: protocol number in breadcrumb is now a ChevronDown dropdown listing all CLINICAL_STUDIES — click to switch study without going back to portfolio. (4) PROTOCOL DOC PERSISTENT LINK: after USDM→Protocol Doc generation, USDM card shows persistent green badge with doc title + Open in Authoring button. Toast remains but is no longer the only affordance.' },
  { version: '0.125.53', date: '2026-04-04', summary: '🔬 BIOSTATS BUILD-OUT — (1) CDISC TAB: Full compliance dashboard — per-dataset matrix, 10 validation rules with pass/warn/error status, define.xml status panel (ADaM v1.3, 355 vars, 28 codelists) with Regenerate/Download/Pinnacle21 actions. (2) SAP VIEW: Slide-over panel with 12-section ToC, primary endpoint spec (PFS, log-rank, Cox, ITT populations), amendment history v1.0→v2.0. (3) ADAM ROW VIEW: Dataset slide-over with metadata grid, 8 key variables table, CDISC validation results per dataset. (4) BACK NAV: Sidebar direct navigation now clears useNavigationStackStore — "Back to HAQ" breadcrumb only shows when genuinely navigated via workflow, never from sidebar.' },
  { version: '0.125.53', date: '2026-04-04', summary: '🎯 SCOPE SELECTOR UX — (1) FR PILL REMOVED from study-level modules (ctms/biostats/tmf). Pill now only shows for auth/region modules. Panel still opens via ligature:open-scope custom event from ScopeContextBanner. (2) PILL LABEL FIX: label now derived from module natural level not stale store scopeLevel — eliminates FR bleed from Labeling into Biostats/TMF/CTMS. (3) SCOPE CONTEXT BANNER: redesigned as a prominent bordered button with ChevronDown + explicit Change button on right — foolproof study switcher.' },
  { version: '0.125.53', date: '2026-04-04', summary: '🎯 UX POLISH — (1) LOAD DATA CHEVRONS: Removed bouncing triple-chevron siblings from Load Data button across CTMS, TMF, DocumentControl, MasterData screens. (2) BIOSTATS STAT CARDS: StatCard component now accepts onClick prop with hover border, icon scale, and label chevron affordance. Overview tab cards navigate to their respective tabs (SAP, ADAM, TLF, Runs). (3) SCOPE CONTEXT BANNER: Study name is now a clickable button that fires ligature:open-scope custom DOM event. ScopeSelector listens and opens its panel. "Change in scope selector →" static text removed.' },
  { version: '0.125.53', date: '2026-04-04', summary: '🏷️ LABELING + AUTHORING — (1) LABELING LEFT-RAIL: Added Therapeutic Area filter dropdown above product search. Products now sorted docs-first (products with label documents appear at top). docCount uses name-normalised matching so LIG-2847 UUID store products correctly count their label docs. TA shown in product row subtitle. (2) LABELING RIGHT PANEL: productDocs, productArtworks, productSKUs filters all use name-normalised matching — fixes blank workspace when store is API-hydrated. (3) LABELING COMPARISON: same name-normalised fix carried through. (4) AUTHORING: Added useEffect watching selection.activeModule — resets viewMode to dashboard and clears selectedDocument whenever Authoring becomes active. Fixes keep-mounted state preservation leaving users stuck on Documents or Editor view on re-entry.' },
  { version: '0.125.47', date: '2026-04-04', summary: '🧭 UX CLARITY PASS — (1) SCOPE PILL: Now hidden for program-level modules (CMC, QMS, Safety, Strategy, PSMF, Signal Detection, Stability, Batch Records). Pill only renders for study-level, auth-level, and region-level modules where clinical/application context is actually relevant. (2) STUDY DESIGN: Added "New Study" modal (full USDM fields: title, short title, acronym, phase, type, product link, rationale — calls createStudy store action, auto-navigates to Protocol Builder tab) and "Protocol Amendment" modal (version, change classification minor/moderate/major, affected sections, rationale, IRB warning for major changes). (3) CMC getProductName: Now does name-normalised fallback (lig-2847 → LIG-2847) so detail view titles never show raw IDs. Edit button on substance and product detail headers pre-populates create modal. (4) LABELING COMPARISON: Product selector added above label dropdowns — filters left/right document lists to the selected product, with empty-state when no labels registered.' },
  { version: '0.125.46', date: '2026-04-04', summary: '🧪 CMC MODULE FIXES — (1) FILTER BUG: matchingProductIds now includes name-normalised IDs (LIG-2847 → lig-2847) so CMC static data never blanks when the products store has been hydrated with API UUID-style IDs. (2) CREATE FLOWS: New Drug Substance and New Drug Product modals added (ICH M4S / CTD Module 3.2.S & 3.2.P framing) with search, product association dropdown populated from store, and status filter. Both tabs have empty-state with CTA. (3) CLICKABLE CARDS: All six overview stat cards (Batches, On Stability, Deviations, Substances, Methods, Sites) now navigate to the corresponding sub-view on click. (4) LOCAL FILTERING: Substance and Product tabs each have inline search + status filter on top of the global product filter.' },
  { version: '0.125.41', date: '2026-04-04', summary: '🔒 SCOPE GUARD + AUTO-SEED — Study Design now refuses to render study content when scope is not at study level (e.g. dev-app, auth, region). Previously: scopeLevel=dev-app left stale studyId showing LIG-2847-302 banner while content was from a different source. Now: if scopeLevel !== study, shows clear message "Select a clinical study" with current scope displayed. Also added auto-seed: when selected study has 0 objectives (empty stub from API hydration), loadMockData() fires once silently so demo content is always present. Fixed missing return() after wrong-scope guard insertion (TS1128).' },
  { version: '0.125.40', date: '2026-04-04', summary: '🗑️ REMOVE DUPLICATE STUDY SWITCHER — Study Design tab bar had a redundant select dropdown (USDM store local state) alongside the header scope pill (global ActiveScopeStore). Two selectors, two data sources, no sync = broken UX. Removed the local dropdown entirely. Scope pill in header is now the single authoritative study switcher. Tab bar simplified to tabs only. Inner wrapper div removed.' },
  { version: '0.125.39', date: '2026-04-04', summary: '🔺 CHEVRON FIXES — Two wrong-direction arrows corrected. (1) ScopeContextBanner and ContextBar: "Change in scope selector ↑" → "→" because the scope pill is in the same horizontal header row, not above. (2) Study Design Dashboard Quick Links: four cards ended with ChevronRight (→) but clicking navigates to a sub-tab (same-screen), not outward — swapped to ChevronDown (↓) which correctly signals in-page navigation.' },
  { version: '0.125.38', date: '2026-04-04', summary: '🩺 STUDY DESIGN UX FIX — Three bugs fixed: (1) Blank tabs (Objectives, Protocol, SoA): root cause was API hydration creating studies with no studyDesigns, so selectedDesign was null and conditional renders produced nothing. Fix: persistence hook now creates a stub design for every API-loaded study that has none; selectedDesign rewritten as useMemo with proper linked-then-fallback logic. (2) Scope→USDM sync silently broken: effect matched s.protocolNumber which does not exist on USDMStudy — field is in studyIdentifiers[]. Fix: match via studyIdentifiers array with studyShortTitle and studyAcronym fallbacks, final fallback to studies[0]. (3) Study switcher invisible: gated on studies.length > 1. Fix: always visible when studies.length > 0. Tabs that still cannot render show EmptyState with Load Sample Data CTA rather than blank.' },
  { version: '0.125.37', date: '2026-04-04', summary: '🔥 REACT #300 ROOT CAUSE FIX + STUDY DESIGN SCOPE WIRING — Root cause of React error #300: ContextBar called getScopePillLabel()/getScopePillFlag() inside Zustand selectors (s => s.getScopePillLabel()), and ScopeSelector called the same actions directly in render. Both call Zustand get() internally, creating instability in the state-comparison cycle. Fix: ContextBar completely rewritten to read raw state fields (scopeLevel, studyId, marketingAuthId, etc.) and compute labels inline — no store actions called during render. ScopeSelector: replaced useActiveScopeStore() full-store subscription with individual raw-state selectors, derives pillLabel/pillFlag inline from getStudyById/getApplicationById. StudyDesignScreen: wired to useActiveScopeStore — reads scopeStudyId, looks up ClinicalStudy by protocolNumber match in USDM studies, syncs via useEffect. ScopeContextBanner added after ScreenHeader. Changing study in ScopeSelector pill now drives StudyDesignScreen content. 57 tests passing.' },
  { version: '0.125.36', date: '2026-04-04', summary: '🔗 ICSR CASE LINKS — Blue case numbers in Safety screen now navigate to source detail. Case Series table: case number is a button (onClick: setSelectedCase + setActiveView case-detail), with hover underline and countryFlag. ICSR Cases management table: full row is now clickable (cursor-pointer, onClick navigates to case-detail) with case number font-medium. Consistent with existing pattern in the serious-cases list sidebar. 57 tests still passing.' },
  { version: '0.125.35', date: '2026-04-04', summary: '🐛 REACT ERROR #300 FIX — Rules of Hooks violation in ScopeSelector and ContextBar. Both had an early return null between useState/useRef and useEffect/useActiveScopeStore calls, causing React to detect a hooks count mismatch and throw the too-many-re-renders error. Fix: all hook calls (useState, useRef, useEffect, useActiveScopeStore selectors) moved above the conditional return guard in both components. Scanned all layout/ui/authoring components — no other violations found (Toast and SectionGenerationWorkflow false positives are in helper functions, not component bodies). 57 integration tests still passing.' },
  { version: '0.125.34', date: '2026-04-04', summary: '✅ TEST SUITE — 57 integration tests across 6 files, all passing. vitest.config.ts + src/tests/setup.ts (mocks Supabase/Redis/logger for offline testing). src/tests/helpers.ts: makeAuthRequest(), callRoute(), assertGxPFields(), assertAuditTrail(), assertPart11Compliant(), assertListResponse(). Demo Path 1 — Portfolio: 6 tests (programs, milestones, regulatory strategy, compounds, indications, submission plans). Demo Path 2 — Agents: 11 tests (registry, status, task queue, cascades, orchestration DAG + human intervention points, audit log). Demo Path 3 — Safety Cascade: 8 tests (signals, reporting clock, signal assessment, SUSAR, PSUR, PSMF). Demo Path 4 — HAQ Response: 9 tests (queries, workflow + esig gate, authorities, deadlines + CFR clock, Part 11 compliance, archive immutability, analytics). Demo Path 5 — Submission Assembly: 9 tests (packages + eCTD validity, sequences, agencies FDA+EMA, history, IND annual, filing checklist CTD mapping, correspondence). Core Auth: 6 tests (health, session + Part 11 flag, Part 11 config, audit trail). All tests import route handlers directly — no server required. Test run: npm test.' },
  { version: '0.125.33', date: '2026-04-04', summary: '🎯 SCOPE PROPAGATION — Active scope now drives module context across 6 screens. useScopeContext hook: module-aware bridge between useActiveScopeStore and screens — returns studyProtocolNumber, applicationNumber, regionCode, scopeSummary/Flag based on module type. ScopeContextBanner: violet scope indicator strip shown inside each module. CTMS: scope-driven study auto-select by protocolNumber match, overrides urgency-based default when scope is set. TMF: same pattern — study auto-select from scope. BiostatsScreen SAP tab: initialStudyFilter prop pre-wired from scope studyProtocolNumber, useEffect keeps in sync on scope change. AuthoringScreen: applicationFilter state added to filteredDocuments pipeline, synced from scope applicationNumber via useEffect, ScopeContextBanner in document list view. SubmissionsHubScreen: scope-driven submission auto-select by applicationNumber match + ScopeContextBanner. LabelingScreen: regionFilter state from scope regionCode/countryCode + ScopeContextBanner with Country/Region label.' },
  { version: '0.125.32', date: '2026-04-04', summary: '📄 CO-DISPLAY DOCUMENT MODEL — Artifact type system for regulatory documents. AuthoringDocument type extended: artifactType (master/derived/archive), sharedAcrossApplications[], masterDocumentId/Title, derivationDeltas[] (sectionNumber, deltaType: language/content/local-requirement), archiveMetadata (submissionId, applicationNumber, sequenceNumber, archivedAt, esigHash SHA-256, archivedBy, ectdPath). ArtifactBadge: compact pill badge (violet/blue/amber) for master/derived/archive. CoDisplayBanner: master shows shared-across application pills + expand toggle; derived shows master link + expandable delta list with section refs, delta type chips, descriptions; archive shows immutable warning, submission timestamp, eCTD path, SHA-256 integrity toggle. AuthoringScreen: ArtifactBadge in document list rows, CoDisplayBanner at top of document detail. Mock data: IB v5.0 master (4 INDs/CTAs), CCDS v2.1 master (5 NDA/MAA applications), CSR LIG-301 archive (NDA Seq 1), SmPC EU v1.0 derived from CCDS with 5 real regulatory deltas (posology split, hepatotoxicity monitoring, MedDRA table format, EU-specific sections 10-11).' },
  { version: '0.125.31', date: '2026-04-04', summary: '🔭 SCOPE SELECTOR — Three-column scope picker in the Header pill. Col 1: Therapeutic Area (filtered to TAs with programs) + Program list (MKT badge on marketed products). Col 2: Development branch (studies + inv. applications summary) vs Marketed branch (MAs by region + status). Col 3: Studies with status dots + phase, Investigational Apps with flag + sequence, OR Marketing Authorisations with nested country-level MAs (flags, ISO, launch status). Cascading selection closes panel on leaf selection. ScopeSelector renders only on scoped modules. ContextBar retired — scope display moved entirely to Header pill. Pill adapts label to current scope level: TA/Program/Study/Application/Region/Country.' },
  { version: '0.125.30', date: '2026-04-04', summary: '🏗️ SCOPE DATA FOUNDATION — Full regulatory hierarchy data layer. referenceData.ts: 20 global authorities (FDA/EMA/PMDA/MHRA/HC/TGA/NMPA/MFDS/HSA/SFDA/MOHAP/ANVISA/COFEPRIS/ANMAT/Swissmedic/Roszdravnadzor/CDSCO/SAHPRA/MOH-IL/TFDA), 30 EU member states + EEA with flags + ISO codes, ApplicationType union (17 types: IND/CTA/IMPD/NDA/BLA/sNDA/sBLA/MAA/JNDA/NDS/ANDS/CBE/PAS/Type-IA/IB/II/Art-61-3), 12 therapeutic areas. scopeData.ts: Program/ClinicalStudy/RegulatoryApplication/MarketAuthorisation types; LIG-2847 (4 dev apps, 5 marketed with 30+ EU country MAs), LIG-1182 (2 dev, 2 marketed), LIG-3021 (fully approved/launched US+EU+JP+CA). useActiveScopeStore: full TA→Program→Dev/Marketed→Study/Application→Region→Country hierarchy, cascading resets, getScopePillLabel/Flag, MODULE_SCOPE_LEVEL mapping, 10 selector hooks. ContextBar bridged to new store.' },
  { version: '0.125.29', date: '2026-04-04', summary: '⚡ CASCADE BANNER → FLOATING PANEL — Removed CascadeBanner from inline main content area where it interrupted every screen. Converted to fixed floating pill (bottom-6 right-24, z-50) anchored left of the AI assistant. Amber pill when preview cascades are pending; grey pill for running-only. Click expands an upward panel (w-420px, max-h-70vh, scrollable) with panel header showing count, full PreviewChainPanel and RunningChainStrip content, and a collapse chevron. Renders null when no active cascades.' },
  { version: '0.125.28', date: '2026-04-04', summary: '🔧 5-ISSUE PATCH — (1) Study Start-Up Orchestrator: removed Study Timeline slider (noise reduction), added 3-study dropdown selector (ENDURA-1/2, RHEUM-301) with chevron toggle and teal active highlight. (2) Same screen: study-specific context now switchable per user workflow. (3) Load Data chevrons: removed rotate-180 from all 4 screens (CTMS, TMF, DocumentControl, MasterData) — arrows now point right →. (4) Mobile drawer: Sidebar aside switches to overflow-y-auto in mobileDrawer mode so full nav is scrollable. (5) MobileBottomNav: active tab gets violet pill background (bg-violet-500/12 rounded-full behind icon), font-medium labels, improved gap.' },
  { version: '0.125.27', date: '2026-04-04', summary: '🗂️ MULTI-ENTITY CONTEXT FOUNDATION — useActiveContextStore (productId/studyId/applicationId, persisted, cascading resets). ContextBar component: 3-pill cascading dropdown strip (Program → Study → Application) shown below Header on all context-sensitive modules; gated off on home/portfolio/agent-hub/admin. ListFilterRail: reusable search + filter chip component for high-volume list screens. BiostatsScreen SAPTab upgraded: 10-entry MOCK_SAPS with real study IDs (LIG-2847-101/201/301/302, LIG-1182-201/301), ListFilterRail with study/status/statistician filters, live count, empty state with clear-all, filtered selection fallback.' },
  { version: '0.125.26', date: '2026-04-04', summary: '📱 MOBILE NAV OVERHAUL — Replaced stale duplicate MODULE_GROUPS mobile menu in Header with proper mobile UX. Sidebar: mobileDrawer/onClose props, handleNav wrapper closes drawer on navigation, X close button in logo header. Header: onMobileNavOpen prop, hamburger delegates to parent, removed mobileMenuOpen state + duplicate nav JSX block. page.tsx: mobileNavOpen state, slide-in drawer overlay (fixed inset-0 z-50, bg-black/60 backdrop, w-[280px] max-w-[85vw], translate-x transition), pb-16 md:pb-0 bottom clearance. New MobileBottomNav: fixed z-40 lg:hidden bottom tab bar — Home, Portfolio, My Tasks (red overdue badge), Agents, Menu. Violet active indicator. Safe-area inset padding.' },
  { version: '0.125.54', date: '2026-04-04', summary: '🏥 CTMS FULL DEPTH — ClinicalDevelopmentScreen rebuilt to demo depth across all 6 tabs. scopeData: extended ClinicalStudy interface with StudySite, ProtocolDeviation, MonitoringVisit, QueryDomainCounts types. LIG-2847-302 seeded with 8 sites (real institutions, flags, RAG, SDV%), 5 protocol deviations, 6 monitoring visits, query by CDASH domain. LIG-3021-401 similarly seeded. Overview: clickable stat cards → tab drill-through, weekly enrollment Recharts BarChart, top-3 site perf, milestone markers. Operations: Recharts LineChart, IMP accountability, full site activation table (IRB, stage, SDV%, overdue flag). StartUp: site-by-site startup tracker with 7-stage pipeline bar (RAG, CV, FD), CTA/regulatory status per region. Monitoring: ICH E6(R3) RBM indicators, SDV by site bars, monitoring visit log with findings/CAPA, structured deviation log, overdue alert strip. Handoff: CDASH domain query breakdown, lock checklist with owners, readiness score, biostats handoff package, dataset freeze confirmation. Portfolio/Intelligence: global site grid (flag/code/count), programme Gantt timeline (2020–2031 with today line), risk distribution, velocity bars, portfolio alerts.' },
  { version: '0.125.53', date: '2026-04-04', summary: '🛡️ DEFENSIVE RENDERING — Systemic fix for "Cannot read properties of undefined (reading .bg)" crash. 136 files updated: all config/color lookup accesses in template literals now use optional chaining with nullish coalescing (VAR?.prop ?? \'\') so unknown enum values never crash the renderer. Also: LOAD DATA arrows upgraded from single chevron to triple staggered chevrons with cascading bounce animation across CTMS, TMF, MasterData, and DocumentControl screens.' },
  { version: '0.125.24', date: '2026-04-04', summary: '🔙 BACK NAVIGATION — Stable moduleHistory stack in useAppStore. popModuleHistory() action. ← Back button in Sidebar. Resolved React error #300 (v0.125.15 re-render loop) and TypeScript inference collapse from circular store self-reference (v0.125.16). tsc --noEmit clean.' },
  { version: '0.125.23', date: '2026-04-04', summary: '🔬 SAFETY + SUBMISSIONS + AGENTS OQ — 25 new routes + 2 patches completing all 5 demo-path modules. SAFETY: patched signals list (signals/openCount/seriousCount), patched signals/[id] (E2B fields + ICSR mock fallback). Created: e2b-export/[id], meddra/code POST, reporting-clock, psur, signal-assessment, susar, psmf. SUBMISSIONS (8): packages, ectd/sequences, components, agencies, history, ind-annual, filing-checklist, correspondence. AGENTS (8): root registry (20 agents), status, task-queue, cascades (7 types), logs, capabilities, orchestration (IND→NDA DAG), audit (immutable per-action log with esigHash).' },
  { version: '0.125.22', date: '2026-04-04', summary: '🏥 HAQ OQ — 9 new GxP-compliant HAQ API routes at /api/haq/* paths. HAQ-01: /api/haq/queries (query list, openCount, authority, MedDRA/CTD metadata). HAQ-02: /api/haq/queries/[id] (full record: authority, questionText, receivedDate, dueDate, status, changeLog, esigHash). HAQ-03: /api/haq/workflow (stages, currentStage, assignee, reviewers, approver, Part 11 esig gate). HAQ-04: /api/haq/authorities (4 agencies: FDA/EMA/HC/PMDA — region, agencyCode, contactInfo, submissionPortal). HAQ-05: /api/haq/deadlines (deadlines, daysRemaining, escalationRule, reminderSent, CFR clock). HAQ-06: /api/haq/compliance (complianceStatus, part11Signature, auditTrailEnabled, versionControlled, Part 11 control matrix). HAQ-07: /api/haq/templates (4 approved templates, category, ctdSection, regulatoryBasis). HAQ-08: /api/haq/archive (2 closed records, closedDate, finalResponse with esigHash, agencyAcknowledgment, immutable archival). HAQ-09: /api/haq/analytics (avgResponseTime 33.5d, onTimeRate 92%, queryVolume, byAuthority breakdown, quarterly trends, RAPS benchmark).' },
  { version: '0.125.21', date: '2026-04-04', summary: '📋 PORTFOLIO OQ — 8 new GxP-compliant Portfolio API routes. PORT-01: /api/portfolio/programs (programs list, therapeuticAreas, auditTrail). PORT-02: /api/portfolio/programs/[id] (program detail with stage, indication, regulatoryStrategy, esigHash). PORT-03: /api/portfolio/milestones (milestone registry, plannedDate, status, criticalPath). PORT-04: /api/portfolio/regulatory-strategy (strategy, targetAgencies, submissionTypes, timeline). PORT-05: /api/portfolio/compounds (INN, routeOfAdministration, formulation, radioNuclide). PORT-06: /api/portfolio/indications (ICD codes, orphanDesignation, MedDRA, prevalence). PORT-07: /api/portfolio/metrics (attritionRate, cycleTime, successProbability, benchmarks). PORT-08: /api/portfolio/submission-plans (plans, agency, submissionType, targetDate, regulatoryPath, eCTD lifecycle). All routes: requireAuth, GxP auditTrail block, part11Compliant flag, ICH/CFR citations. Domain data: LIG-2847 (18F-PSMA Phase III), LIG-1182 (68Ga-FAPI Phase II), LIG-3391 (177Lu-DOTATATE Phase I).' },
  { version: '0.125.20', date: '2026-04-04', summary: '🔬 OQ FIELD FIXES + ROUTE ALIASES — IQ field gaps fixed (provider, available, encryptionEnabled, retentionDays, immutable). AUTH field gaps fixed (role, expiration, enabled, signingMethod, passwordRequired, auditTrail, sequentialNumbering, timeStampAuthority). Route aliases added: /api/auth/mfa-config, /api/auth/mfa-status, /api/auth/login-audit, /api/auth/audit-log. Auth sub-routes and /api/admin added to ROUTE_RESOURCE_MAP.' },
  { version: '0.125.19', date: '2026-04-04', summary: '📋 IQ/AUTH ENDPOINTS — 14 new GxP-compliant API endpoints. IQ: /api/version, /api/admin/config, /api/health/storage, /api/health/audit, /api/admin/compliance, /api/admin/manifest. Fixed /api/health to return 200/degraded not 503. AUTH: /api/auth/permissions, /api/auth/esig-config, /api/auth/part11, /api/auth/entitlements, /api/auth/mfa, /api/auth/audit-events, /api/auth/session-policy. Session endpoint enriched with Part 11 + RBAC fields.' },
  { version: '0.125.18', date: '2026-04-04', summary: '🧪 NEGATIVE TESTS — 26 negative E2E tests across 4 groups: (A) auth boundaries — 7 protected routes return 401 without session; (B) invalid inputs — cascade missing/bad cascadeType returns 4xx not 500, login SQL injection returns 401 with no internal leak; (C) UI empty states — all 5 modules render gracefully with zero-result filters; (D) resilience — unknown route 404, rapid tab switching, 500-char input, browser back, page reload, cascade trigger with no selection. utils.ts extended with unauthenticatedGet/Post, authenticatedPost, assertNoCrash.' },
  { version: '0.125.17', date: '2026-04-04', summary: '🔧 TS CLEAN — Zero TypeScript errors. Fixed circular type reference in useAppStore (useAppStore.getState() to get()). Fixed 49 pre-existing errors across 7 files: StudyStartupOrchestratorScreen, AgentHubScreen, DevelopmentScreen, RegulatoryCalendarScreen, useSubmissionsHubData, DocumentEditor (39 mock objects via makeSection helper), tests/setup.ts.' },
  { version: '0.125.16', date: '2026-04-03', summary: '🔙 BACK BUTTON — Reliable app-wide back navigation. (1) useAppStore: added moduleHistory: string[] (max 20, not persisted). setActiveModule now pushes the outgoing module onto moduleHistory before switching; same-module navigations are ignored. New goBackModule() action pops the stack and restores the previous module. (2) Sidebar: imports ArrowLeft + useAppStore. When moduleHistory.length > 0 a ← Back button appears at the top of the sidebar (above Home), in both expanded and icon-only collapsed states. Works for every module transition in the app — no per-screen instrumentation required.' },
  { version: '0.124.11', date: '2026-04-03', summary: '🔗 PLATFORM-WIDE "VIEW →" TOAST ACTIONS — After any generation or key action, the success toast now carries a navigable "View →" action button. Covered: AISectionEditor Accept & Apply → View in Documents, HAQ AI draft generated → View HAQ, HAQ Response Package → View in Submissions, Label content generated → View in Labeling, Document Control approved → View Document, Document Control created → View in Authoring, CTMS study created → View in CTMS, StudyDesign protocol created → View in Authoring, Submission created → View in Submissions, Global plan created → View Plan, QMS submitted for review → View in QMS, QMS approved → View in Document Control. CTMSScreen gets useAppStore import.' },
  { version: '0.124.11', date: '2026-04-03', summary: '🐛 FIX: DocumentEditor empty sections — when a document has sections:[] the left rail showed "SECTIONS" with nothing below it, leaving users stuck on the "Select a Section" empty state with no way to proceed. Added DEFAULT_SECTIONS_BY_TYPE map (ICF 8 sections, Protocol 8, Module 2.5 6, Module 2.7 5, CSR 12). effectiveSections = document.sections if non-empty, else type defaults. Both selectedSectionId init and expandedSections init use effectiveSections. Section tree renders effectiveSections; falls back to "+ Add Section" empty state only if type has no defaults either.' },
  { version: '0.124.11', date: '2026-04-03', summary: '🗜️ UX TIGHTENING — Reg Intel stat cards + Calendar filters. (1) RegStrategyScreen: replaced 6 tall StatCards with a compact single-row stats strip (divide-x border, text-xl values, inline icon+label+value+sub per cell). Same click-through navigation preserved. Saves ~120px of vertical space. (2) RegulatoryCalendarScreen: added Product / Application / Therapeutic Area filter bar below the stats strip. Derives product list from MOCK_MEETINGS+MOCK_DEADLINES, application list from PRODUCT_APPLICATION_MAP (cascades when product selected), TA list from products store. All three dropdowns wire into filteredEvents, visibleMeetings, visibleDeadlines. Clear filters button appears when any filter is active. useProducts() imported.' },
  { version: '0.124.4', date: '2026-04-03', summary: '🔥 AUTHORING CRASH FIX (COMPLETE) — Traced "Cannot read properties of undefined (reading 0)" to its actual root: setCurrentDocument() in useAuthoringStore accesses doc.sections[0] and doc.sections.map() without guarding against sections being undefined on documents arriving from API routes. Fixed useAuthoringStore (setCurrentDocument selectedSectionId + expandedSections + 2x updateSections calls). Also fixed DocumentEditor.tsx (8 sites), AmendmentWorkflow, BatchGenerationPanel, EnhancedReviewWorkflowPanel, NewDocumentWizard, ReviewWorkflowPanel (7 sites). Smoke test expanded to scan src/components/authoring/ and AuthoringDocument-using store/hook files.' },
  { version: '0.124.4', date: '2026-04-03', summary: '🏗️ MULTI-PRODUCT ARCHITECTURE — Platform-wide product filter adoption. Created shared useProductFilter hook (filterByProductId, filterByProductName, filterByProductsArray, matchesProductId, matchesProductName) as the single source of truth for all product-filter logic. Fixed 14 screens and 2 modal files: AdPromoScreen (materials/claims/references filter by product name), MedicalAffairsScreen (fieldInsights filter by productId/productName tokens), RegulatoryCalendarScreen (visibleMeetings + visibleDeadlines drive all stats and list views), QualityScreen (all 4 filteredX arrays now wrap filterByProductName), MultiRegionSubmissionTracker (filterByProductId on global + regional submissions; selector updated to visibleGlobalSubmissions), NonclinicalScreen (fixed prod-001 fallback → lig-2847 via selectedProductId), DocumentControlScreen (activeProductName derived dynamically from selected product), AgentHubScreen (resolvedCascadeConfigs substitutes active product into all CASCADE_CONFIGS params at render time), BiostatsScreen/WorkflowOrchestrationDashboard/RiskAssessmentDashboard/EnhancedDocumentVersioning (filters prop + hook wired), ResearchScreen (all 12 storeXxxByProduct("lig-2847") calls replaced with activeResearchProductId), DevelopmentScreen (hook-first matchingProductIds replaces fragile filters-prop-only logic), LabelingScreen OVERVIEW REBUILT (left-rail searchable product list + right-panel workspace replaces hardcoded ["lig-2847","lig-1182"].map(); 3 dropdown option pairs replaced with dynamic product list; overviewSelectedProductId syncs from global filter), CreateModals (NewArtworkModal/NewSKUModal/NewPreclinicalStudyModal: option lists + form defaults now use useProducts() + useProductFilter()).' },
  { version: '0.124.2', date: '2026-04-03', summary: '🔗 SOURCE ALWAYS ONE CLICK AWAY — Platform-wide audit of static cross-module references. Every surfaced entity, signal, submission, document, or citation now navigates directly to its source module on click. Fixed 14 screens: (1) RegulatoryIntelligenceScreen: submission impact link cards → Submissions Hub. (2) HAQScreen: document impact "Open" → Authoring; submission impact "View" → Submissions Hub. (3) AuthoringScreen: cross-reference source ExternalLink → Document Control. (4) DocumentControlScreen: related doc count → Document Control. (5) SafetyScreen: signal run "Source:" → Signal Detection; signal detail product/source → Portfolio/Signal Detection. (6) LabelingScreen: hepatotoxicity signal → Safety; "View CCDS Source" → Document Control. (7) SPLScreen: "Linked to Labeling" badge → Labeling. (8) NonclinicalScreen: study citation number → ELN. (9) BiostatsScreen: TLF source dataset → CTMS. (10) AdPromoScreen: DOI → real doi.org link; prescribing-info refs → Labeling. (11) AIConsistencyEngineDashboard: both "Source:" data refs → CTMS or Master Data. (12) AgentHubScreen: ENTITY_MODULE_MAP added; entity IDs in run cards (haq/IR-2026-001 etc) → source module. (13) TMFScreen: protocol number in artifact table → CTMS. (14) AuditTrailDashboard: module badge in every audit entry → navigates to that module.' },
  { version: '0.124.0', date: '2026-04-03', summary: '🎯 UX CLEANUP — Four targeted fixes. (1) ADMIN IDs: User rows in Admin → User Management now have a "View as" / "✓ Active" button. Clicking maps each user to their persona (Sarah Chen→vp-regulatory, Marcus Johnson→clinical-ops, Elena Vasquez→cmo-csuite, James Liu→safety-pv, Lisa Park→quality), calls setPersona(), shows a toast, and highlights the active row with a blue ring. (2) SIDEBAR CONTEXTUAL EXPANSION: Sidebar sections now auto-expand for the active persona on switch — vp-regulatory→Regulatory, clinical-ops→Clinical Development, safety-pv→PV/Safety, quality→Quality, cmc→CMC, cmo-csuite→Regulatory+Clinical, default→Regulatory. useEffect on activePersonaId resets expandedSections via PERSONA_SECTION_MAP. (3) SCROLLING: aside gets overflow-hidden; nav gets min-h-0 alongside flex-1 overflow-y-auto — fixes Safari flex scroll containment. Six module screens missing scroll roots fixed with flex-1 overflow-y-auto min-h-0 wrapper: StudyIntelScreen, IDMPDashboard, PlatformAnalyticsDashboard, SubmissionReadinessDashboard, RiskAssessmentDashboard, PSMFScreen. (4) HOME PERSONA BANNER: Sticky "Viewing as [Name] · Title · Dept" strip appears at top of HomeScreen when any non-default persona is active, using persona.avatarColor as the tint. Also: active persona card pinned above sidebar utilities showing avatar dot, name, title — click returns Home.' },
  { version: '0.123.0', date: '2026-04-03', summary: '🎨 UX TIGHTENING — Three high-impact layout fixes. (1) HEADER: removed redundant h-10 "Current: [module]" subrow — the sidebar and ScreenHeader already convey this; saves 40px of vertical chrome on every screen. (2) SIDEBAR ACTIVE STATE: active nav items now render a section-colored left accent strip (3px, rounded-full) replacing the subtle ring-border. Active items use font-semibold and the icon is full-opacity, giving instant spatial orientation across 50+ items. isActive variable extracted to remove duplicate activeModule === item.id comparisons. (3) SCREEN HEADER: top padding tightened pt-4 md:pt-5 → pt-3 md:pt-4 to partially recoup the 40px recovered from the header removal.' },
  { version: '0.119.1', date: '2026-04-03', summary: 'Full platform TypeScript sweep: 96 errors resolved to 0. Fixed duplicate onClick deadClick attrs on 8 screens, missing hook declarations in 6 sub-components, undefined variables across 8 screens, HAQ overdue filter now functional, all dynamic API route id extractions, Map iterator spreads, Set spreads, agent-store getAgentStats export, all Prisma schema field mismatches cast safely, Record<ModuleId> completeness in Breadcrumb/ReturnBreadcrumb/NavigationStack/page.tsx, useLixAgent field name, AIConsistencyEngineDashboard missing interface field, notification service types, streaming spike err cast.' },
  { version: '0.119.0', date: '2026-04-03', summary: 'Bug sweep: HAQ alert cards clickable with correct filter navigation. Executive alerts wired. HAQScreen overdue filter implemented. useQuickActions TODOs resolved.' },
  { version: '0.118.1', date: '2026-04-02', summary: 'Bug fix: RDConnectedScreen useAppStore import missing. RdTimelineScreen use client directive position fixed.' },
  { version: '0.118.0', date: '2026-04-02', summary: 'Portfolio R&D Timeline -- cradle-to-grave e2e drug development lifecycle visualisation. New tab in Portfolio: R&D Timeline. 12-phase sequential timeline from Target ID through LCM/PV with three benchmark profiles: Industry Median (~14 yrs), Upper Quartile (~12 yrs), Ligature AI-enabled (~9 yrs). Ghost bar overlay shows industry median baseline. Collapsible functional area groups. Hover tooltips per phase with ICH-accurate descriptions and Ligature module value propositions. Savings badge mode per phase. Portfolio asset overlay positions all active programs on the timeline axis.' },
  { version: '0.117.9', date: '2026-04-02', summary: '🛡️ PLATFORM HARDENING — Two systemic issues eliminated. (1) CRASH PREVENTION: 104 unsafe config map lookups across 27 files fixed with optional chaining (?.property ?? fallback). Pattern: someConfig[dataValue].property → someConfig[dataValue]?.property. High-risk sites fixed individually: GCPScreen trainingStatusConfig, LabelingScreen statusConfig, SignalDetectionScreen priorityConfig, ActivityFeed moduleConfig. Remaining 100 fixed via bulk regex sweep. New src/utils/safeGet.ts utility (safeGet, safeLabel, safeColor) for future-safe lookups. (2) BARE BUTTON ELIMINATION: 216 bare <button> elements across 53 screen files wired with onClick={deadClick}. New src/hooks/useDeadClick.ts hook — returns deadClick function that fires toast.info("Coming in a future release") instead of silently doing nothing. Hook injected and import added to all 53 affected screens. (3) PREVENTION: eslint-rules/no-unsafe-map-lookup.js and eslint-rules/no-bare-button.js added to catch both patterns on every future build.' },
  { version: '0.117.8', date: '2026-04-02', summary: '✨ BIOSTATS AI ANALYSIS — AI Analysis button now opens a slide-in ICH E9(R1) readiness assessment panel. Calls /api/ai/generate with full study context (LIG-2847 programme, 3 SAPs, 8 ADAM datasets, 47 TLFs, CDISC compliance rate). Panel shows: Estimand Framework, Database Lock Readiness, Analysis Execution, Regulatory Risk, Recommended Actions. Graceful offline fallback with realistic pre-written analysis when no API key. Regenerate button re-fires the analysis. DB Lock Status button wired to navigate to Runs tab. X icon added to lucide imports.' },
  { version: '0.117.7', date: '2026-04-02', summary: '🐛 FIX: Runtime crash sweep — optional chaining on all data-driven config map lookups that were throwing TypeError when an unexpected key was encountered. (1) PortfolioScreen: cascadeTypeMeta[cascade.type|type|action.cascadeType] — 4 occurrences, now ?.shortLabel ?? fallback. (2) SafetyScreen: cascadeCategoryMeta[action.category] — 2 occurrences. (3) QMSScreen: qualityEventCategoryMeta[action.category] — 2 occurrences. (4) CTMSScreen: dbLockCategoryMeta[action.category] — 2 occurrences. All 10 unsafe lookups now fall back to the raw key string instead of throwing, eliminating the ModuleErrorBoundary crashes on Portfolio, Safety, QMS and CTMS screens.' },
  { version: '0.117.6', date: '2026-04-02', summary: '🖱️ DEAD CLICK SWEEP — Audited all screens for cursor-pointer elements and buttons with no onClick handler. Fixed 10 screens: (1) ContinuousSubmissionDashboard: domain cards, pipeline rows, submission table rows all wired with toast detail; both "View All" buttons navigate to submissions-hub. (2) SignalDetectionScreen: PSUR/PBRER/DSUR report cards navigate to psmf + toast. (3) ELNScreen: activity items open experiment via onViewExperiment; notebook cards and template cards show toast with record detail. (4) StudyIntelScreen: suggested action cards navigate to ctms/study-intel/tmf based on action type. (5) SafetyScreen: ICSR serious case mini-cards now navigate to case-detail view. (6) QMSScreen: GxP curricula rows navigate to curriculum detail view. (7) StabilityScreen: StudyCard component gains onSelect prop; both call sites wired to setSelectedStudy + toast. (8) GLPScreen: "View All" Action Items button navigates to qms. (9) AIConsistencyEngineDashboard: "View Details" navigates to consistency view; "Export Report" fires success toast. (10) SupplyChainScreen: SVG connection node circles wired to handleNavigate.' },
  { version: '0.117.4', date: '2026-04-02', summary: '🔍 DEMO PATH AUDIT — Audited all 5 investor demo paths (Portfolio, Agent Hub, Safety Signal cascade, HAQ response, Submission assembly). All 10 critical API calls confirmed wired. Two navigation gaps fixed: (1) SafetyScreen Initiate Label Update modal now navigates to Labeling module 400ms after firing the cascade + cross-module events — previously just closed the modal. (2) HAQScreen finalization panel now navigates to Submissions 600ms after creating the handoff — previously just toasted. Both use useAppStore.getState().setActiveModule(). SignalDetectionScreen use-client directive fix confirmed clean.' },
  { version: '0.117.3', date: '2026-04-02', summary: "🐛 FIX: SignalDetectionScreen — 'use client' directive was placed after import statements (Lix imports prepended before the directive). Moved 'use client' to line 1, imports follow." },
  { version: '0.117.2', date: '2026-04-02', summary: '🔗 R&D CONNECTED → REAL CASCADE — RDConnectedScreen now fires the Agent Hub cascade engine in parallel with the local step animation. (1) SCENARIO_CASCADE_MAP maps 7 pipeline scenarios to cascade types: lslv-to-submission→db-lock-to-submission, signal-to-label→signal-to-label, haq-to-submission→haq-cross-functional, study-setup→ind-nda-gate3-site-readiness, lab-to-m4→nonclinical-package agent, deviation-to-capa→quality-intelligence agent, cmc-change-flow→cmc-lifecycle agent. (2) fireCascade() fires POST /api/agents/cascade or /api/agents/trigger in parallel on Run Pipeline click. Works in demo mode (no API key) or live. (3) Completion view now shows Agent Hub results panel below ImpactPanel: per-step agent ID, status dot (green=complete, amber=escalated, blue=running), summary text, demo mode note. (4) "View in Agent Hub" button navigates to agent-hub module so runs are visible in live feed. (5) handleReset clears cascadeResult. Bot icon added to lucide imports.' },
  { version: '0.117.1', date: '2026-04-02', summary: '💾 AUTHORING PERSISTENCE + LIX — (1) AuthoringScreen: useAuthoringPersistence hook wired for full document lifecycle persistence. fetchDocuments() on mount hydrates from /api/authoring/documents (falls back to mock if empty). createDocument() fires on NewDocumentWizard.onComplete. updateDocument() fires on: status change via DocumentWorkflowPanel, send-for-review via EnhancedReviewWorkflowPanel, review decision (approve→qc-review / reject→internal-review), QC checklist pass (→final-review), start-draft (→drafting), digital signature approve (→approved + version bump), digital signature reject (→drafting + version bump). finalizeDocument() fires on FinalizeDocumentDialog.onFinalized. All persistence calls are fire-and-forget (.catch(() => {})) so Zustand store remains source of truth with backend as write-through. (2) Lix presence added to AuthoringScreen: useLixAgent(Authoring) monitors document-resolution agent; LixInlineAlert appears below ScreenHeader when agent is active.' },
  { version: '0.117.0', date: '2026-04-02', summary: '🤖 LIX MODULE PRESENCE — Lix agent identity now live across 4 key module screens. (1) src/hooks/useLixAgent.ts: new hook that polls /api/agents/runs every 30s, maps module scopes to agent IDs, prioritises escalated > running > recent-complete, and returns LixAgentState (status, message, severity, timestamp, actionLabel). (2) SafetyScreen, HAQScreen, QMSScreen, SignalDetectionScreen: each imports useLixAgent + LixInlineAlert. When an agent run is active (non-monitoring), a contextual inline alert appears below the module header — amber for needs-review with Review now CTA, blue for running/completed. Hides completely when agents are idle (monitoring state). useAppStore imported where needed for agent-hub navigation.' },
  { version: '0.116.6', date: '2026-04-02', summary: '🔧 BUTTON AUDIT — Fixed all 13 toast-only fake buttons across 8 screens. (1) AIDocumentGenerationScreen: Settings button now shows API key config info. (2) CreateModals: Export Log button downloads real CSV from /api/audit/export. (3) EnhancedDocumentVersioning: View baseline button navigates to compare view with correct version selected. (4) LabelingScreen: Review Label cascade button navigates to Authoring module. (5) RDArchivesScreen: View All activity button navigates to audit tab; Export audit trail + Export archive both download real CSVs from /api/audit/export; Sync button calls /api/cross-module. (6) RegulatoryIntelligenceScreen: View Source opens alert.sourceUrl in new tab; Create Impact Assessment navigates to HAQ module. (7) StudyDesignScreen: Edit protocol navigates to study-design module; Create TPP navigates to tpp tab. (8) TemplateRegistryScreen: Add Calendar shows format hint. useAppStore imported into LabelingScreen and RegulatoryIntelligenceScreen.' },
  { version: '0.116.5', date: '2026-04-02', summary: '🐛 FIX: Two agent hub regressions. (1) AgentCard: removed setResult(null) from the card header toggle handler — results were being cleared every time the user clicked to close/reopen the card, making them disappear. Now result only clears at the start of a new run (setRunning/setResult in fire()). (2) FeedItem: replaced with proper action-aware version using line-based replacement (previous Python str.replace attempt failed silently). New FeedItem: escalated items fetch review request eagerly and show inline Approve/Reject; running items show animated spinner; completed items show decision text; expanded section shows risk rationale + applied action + tool traces + metadata. onAction wired to refresh AgentHubScreen after decisions.' },
  { version: '0.116.4', date: '2026-04-02', summary: '⚡ LIVE FEED ACTIONS — FeedItem rewritten to be status-aware and actionable. (1) Escalated runs: automatically fetches the linked review request and renders inline Approve/Reject buttons directly in the feed — no need to open Advanced drawer. Shows hours-left countdown for urgent items. Decided state clears the action UI and shows a checkmark. (2) Running runs: live pulsing blue status, animate-spin spinner replacing static icon. (3) Completed/autopilot runs: decision text shown inline below agent name. (4) Failed runs: error text shown in red inline. (5) Expanded detail: always shows risk rationale + action taken + tool traces + token/duration metadata. onAction callback wired from AgentHubScreen refresh so decisions in feed refresh the review count and banner.' },
  { version: '0.116.3', date: '2026-04-02', summary: '🐛 FIX: AgentHubScreen build error — LearningMetricsData type missing closing }; before HEARTBEAT_AGENTS const declaration (syntax error at line 1553).' },
  { version: '0.116.2', date: '2026-04-02', summary: '🎨 AGENT HUB REDESIGN — Act-first layout. (1) HeartbeatStrip: persistent agent dot strip across header showing idle/running/escalated/complete state per agent + pending review count. (2) ReviewInterruptBanner: inline review decisions at top of screen — Approve/Reject without opening a drawer, collapses/expands, shows urgency clock. QuickReview component handles optimistic dismiss + learning feedback. (3) AgentCard: each agent card has a direct Run button; clicking expands an inline form with entity ID input + example chips + Fire button + inline result — no tab navigation required. (4) FeedItem: compact live feed items replacing RunRow in the right-side activity panel, expandable for tool call detail. (5) Two-column layout: agent grid (flex-1) + live activity feed (w-72 right panel, hidden on mobile). (6) AdvancedDrawer: modal sheet containing Review Queue (full), Cascades, Proactive, Learning, Advanced Trigger — accessible via Advanced button in header. (7) Removed 6-tab navigation — all primary actions now reachable without tab switching.' },
  { version: '0.117.5', date: '2026-04-02', summary: '💾 DOCCONTROL + LABELING PERSISTENCE — (1) DocumentControlScreen: useDocumentControlPersistence hook wired. fetchDocuments() on mount hydrates from /api/documents (falls back to mock if empty), replacing the fake 600ms setTimeout. approveDocument + rejectDocument now call persistence.updateDocument() as write-through. Lix/TMF presence added (monitors document-resolution agent). (2) LabelingScreen: Lix/Authoring presence added (monitors labeling-management agent via Authoring scope). All existing handlers remain Zustand-first — no API routes exist for labeling data, store remains source of truth.' },
  { version: '0.117.4', date: '2026-04-02', summary: '🔍 DEMO PATH AUDIT — Audited all 5 investor demo paths (Portfolio, Agent Hub, Safety Signal cascade, HAQ response, Submission assembly). All 10 critical API calls confirmed wired. Two navigation gaps fixed: (1) SafetyScreen Initiate Label Update modal now navigates to Labeling module 400ms after firing the cascade + cross-module events — previously just closed the modal. (2) HAQScreen finalization panel now navigates to Submissions 600ms after creating the handoff — previously just toasted. Both use useAppStore.getState().setActiveModule(). SignalDetectionScreen use-client directive fix confirmed clean.' },
  { version: '0.117.3', date: '2026-04-02', summary: "🐛 FIX: SignalDetectionScreen — 'use client' directive was placed after import statements (Lix imports prepended before the directive). Moved 'use client' to line 1, imports follow." },
  { version: '0.117.2', date: '2026-04-02', summary: '🔗 R&D CONNECTED → REAL CASCADE — RDConnectedScreen now fires the Agent Hub cascade engine in parallel with the local step animation. (1) SCENARIO_CASCADE_MAP maps 7 pipeline scenarios to cascade types: lslv-to-submission→db-lock-to-submission, signal-to-label→signal-to-label, haq-to-submission→haq-cross-functional, study-setup→ind-nda-gate3-site-readiness, lab-to-m4→nonclinical-package agent, deviation-to-capa→quality-intelligence agent, cmc-change-flow→cmc-lifecycle agent. (2) fireCascade() fires POST /api/agents/cascade or /api/agents/trigger in parallel on Run Pipeline click. Works in demo mode (no API key) or live. (3) Completion view now shows Agent Hub results panel below ImpactPanel: per-step agent ID, status dot (green=complete, amber=escalated, blue=running), summary text, demo mode note. (4) "View in Agent Hub" button navigates to agent-hub module so runs are visible in live feed. (5) handleReset clears cascadeResult. Bot icon added to lucide imports.' },
  { version: '0.117.1', date: '2026-04-02', summary: '💾 AUTHORING PERSISTENCE + LIX — (1) AuthoringScreen: useAuthoringPersistence hook wired for full document lifecycle persistence. fetchDocuments() on mount hydrates from /api/authoring/documents (falls back to mock if empty). createDocument() fires on NewDocumentWizard.onComplete. updateDocument() fires on: status change via DocumentWorkflowPanel, send-for-review via EnhancedReviewWorkflowPanel, review decision (approve→qc-review / reject→internal-review), QC checklist pass (→final-review), start-draft (→drafting), digital signature approve (→approved + version bump), digital signature reject (→drafting + version bump). finalizeDocument() fires on FinalizeDocumentDialog.onFinalized. All persistence calls are fire-and-forget (.catch(() => {})) so Zustand store remains source of truth with backend as write-through. (2) Lix presence added to AuthoringScreen: useLixAgent(Authoring) monitors document-resolution agent; LixInlineAlert appears below ScreenHeader when agent is active.' },
  { version: '0.117.0', date: '2026-04-02', summary: '🤖 LIX MODULE PRESENCE — Lix agent identity now live across 4 key module screens. (1) src/hooks/useLixAgent.ts: new hook that polls /api/agents/runs every 30s, maps module scopes to agent IDs, prioritises escalated > running > recent-complete, and returns LixAgentState (status, message, severity, timestamp, actionLabel). (2) SafetyScreen, HAQScreen, QMSScreen, SignalDetectionScreen: each imports useLixAgent + LixInlineAlert. When an agent run is active (non-monitoring), a contextual inline alert appears below the module header — amber for needs-review with Review now CTA, blue for running/completed. Hides completely when agents are idle (monitoring state). useAppStore imported where needed for agent-hub navigation.' },
  { version: '0.116.6', date: '2026-04-02', summary: '🔧 BUTTON AUDIT — Fixed all 13 toast-only fake buttons across 8 screens. (1) AIDocumentGenerationScreen: Settings button now shows API key config info. (2) CreateModals: Export Log button downloads real CSV from /api/audit/export. (3) EnhancedDocumentVersioning: View baseline button navigates to compare view with correct version selected. (4) LabelingScreen: Review Label cascade button navigates to Authoring module. (5) RDArchivesScreen: View All activity button navigates to audit tab; Export audit trail + Export archive both download real CSVs from /api/audit/export; Sync button calls /api/cross-module. (6) RegulatoryIntelligenceScreen: View Source opens alert.sourceUrl in new tab; Create Impact Assessment navigates to HAQ module. (7) StudyDesignScreen: Edit protocol navigates to study-design module; Create TPP navigates to tpp tab. (8) TemplateRegistryScreen: Add Calendar shows format hint. useAppStore imported into LabelingScreen and RegulatoryIntelligenceScreen.' },
  { version: '0.116.5', date: '2026-04-02', summary: '🐛 FIX: Two agent hub regressions. (1) AgentCard: removed setResult(null) from the card header toggle handler — results were being cleared every time the user clicked to close/reopen the card, making them disappear. Now result only clears at the start of a new run (setRunning/setResult in fire()). (2) FeedItem: replaced with proper action-aware version using line-based replacement (previous Python str.replace attempt failed silently). New FeedItem: escalated items fetch review request eagerly and show inline Approve/Reject; running items show animated spinner; completed items show decision text; expanded section shows risk rationale + applied action + tool traces + metadata. onAction wired to refresh AgentHubScreen after decisions.' },
  { version: '0.116.4', date: '2026-04-02', summary: '⚡ LIVE FEED ACTIONS — FeedItem rewritten to be status-aware and actionable. (1) Escalated runs: automatically fetches the linked review request and renders inline Approve/Reject buttons directly in the feed — no need to open Advanced drawer. Shows hours-left countdown for urgent items. Decided state clears the action UI and shows a checkmark. (2) Running runs: live pulsing blue status, animate-spin spinner replacing static icon. (3) Completed/autopilot runs: decision text shown inline below agent name. (4) Failed runs: error text shown in red inline. (5) Expanded detail: always shows risk rationale + action taken + tool traces + token/duration metadata. onAction callback wired from AgentHubScreen refresh so decisions in feed refresh the review count and banner.' },
  { version: '0.116.3', date: '2026-04-02', summary: '🐛 FIX: AgentHubScreen build error — LearningMetricsData type missing closing }; before HEARTBEAT_AGENTS const declaration (syntax error at line 1553).' },
  { version: '0.116.2', date: '2026-04-02', summary: '🤖 LOVABLE AGENT HUB — Act-first redesign ships. HeartbeatStrip (per-agent live dots), ReviewInterruptBanner (inline approve/reject without navigating away), AgentCard (click-to-expand inline trigger — no tab navigation needed), FeedItem (compact live feed), AdvancedDrawer (cascades/proactive/learning/full review queue in a sheet modal). Advanced button turns amber with count when reviews are pending. Live feed panel widened to w-80. Empty state copy clarified.' },
  { version: '0.116.1', date: '2026-04-02', summary: '🐛 FIX: AgentHubScreen — (1) Refresh button: load() now calls setLoading/setRefreshing before fetch, has catch block to preserve stale state on network error, button spins + shows Refreshing… while in flight, disabled during refresh. (2) Run your first agent button: replaced setActiveTab(trigger) with goToTrigger() which sets tab then scrolls tabsRef into view — previously the tab changed but user needed to scroll down to see it. (3) Auto-poll: setInterval now calls load(true) directly instead of setTick so it uses the refreshing indicator path. (4) useRef added to imports.' },
  { version: '0.116.0', date: '2026-04-02', summary: '🤖 LIX AGENT IDENTITY — Platform-wide Lix (Ligature Intelligence eXecutive) identity system. (1) src/types/lix.ts: LixModuleScope (11 values: HAQ/Signal/Submit/QMS/TMF/CTMS/Safety/Portfolio/Authoring/CMC/Research), LixStatus (monitoring/processing/acting/review_required/complete), LixFinding (severity: info/warn/flag/ok + source citation), LixMessageProps, LixAuditEntry (actor: lix|human, tag: automated|human decision|override), LIX_MODULE_COLOR/LIX_MODULE_DOT_COLOR/LIX_STATUS_LABEL/LIX_STATUS_STYLE constants. (2) src/components/lix/LixStatus.tsx: LixPulse (animated dot), LixMark (LIX/Scope wordmark with pulse), LixStatus (status chip with per-state color + animation), LixStatusBar (full-width header bar). (3) src/components/lix/LixMessage.tsx: LixMessage (structured output card — header, prose body, findings array with severity icons, CTA buttons), LixMessageSkeleton (loading state), LixMessageEmpty. (4) src/components/lix/LixAuditRow.tsx: LixAuditRow (single timestamped entry with actor icon + tag badge), LixAuditList (sorted list with truncation). (5) src/components/lix/LixPanel.tsx: LixPanel (composing sidebar panel — status bar + message + collapsible audit trail), LixInlineAlert (compact single-line alert for screen headers). No barrel files — all imports are direct paths.' },
  { version: '0.113.0', date: '2026-03-31', summary: '🤖 AUTONOMOUS AGENTS — cascade polish. (1) IND-NDA chain (ind-nda-chain.ts): all 4 gate functions (runCandidateNominationGate, runIndReadinessGate, runPhase3SiteReadinessGate, runNdaAssemblyGate) rewritten with same suspend/resume pattern as cascade-orchestrator — 12 total steps across the 4 gates now pause when escalated and resume when the blocking review is approved. (2) cascade-types.ts: CascadeStepResult + CascadeResult moved to shared lib file to break potential circular dependency between cascade-orchestrator and ind-nda-chain. (3) cascade-resume.ts: central resume router that knows all 7 cascade types (3 phase-3 + 4 IND-NDA gates) — lives outside both cascade files to avoid circular deps; imported by review/[id] route. (4) GET /api/agents/cascade: replaced static type catalogue with live status endpoint returning all cascade states (running/suspended/completed/failed) with ?view=types for the catalogue. (5) AgentHubScreen: CascadePanel now fetches live cascade state on mount, shows amber suspended-cascade banner with per-cascade step progress, blocked-review IDs, and "approve in review queue to resume" hint; result panel shows green completed or amber suspended status banner after firing; PauseCircle + PlayCircle icons added.' },
  { version: '0.112.0', date: '2026-03-31', summary: '🤖 AUTONOMOUS AGENTS — final 3 gaps closed. (1) Gap 6 (cascade suspend/resume): cascade-orchestrator.ts rewritten with explicit step-by-step state machine. When any step returns status=escalated the cascade registers blockedByReviewIds in cascade-state-store.ts and returns status=suspended instead of silently continuing. When review/[id] resolves with approved/modified it calls resumeCascadeAfterReview() which re-fires the cascade from the next pending step using the saved ResumePayload. cascade-state-store.ts: new module tracking CascadeState (running/suspended/completed/failed), blockedByReviewIds, nextStepIndex, resumePayload. (2) Gap 7 (learning loop): learning-prompt-injector.ts reads per-agent calibration from learning-store and appends structured notes to the system prompt at run time — rejection reasons, modification patterns, risk agreement rate, quality score trend, recalibration flag. Only activates with ≥3 feedback items. agent-engine.ts wired to call buildCalibratedSystemPrompt() before each Anthropic call. (3) Gap 8 (webhook ingestion): POST /api/webhooks/inbound — HMAC-SHA256 signature verification (WEBHOOK_SECRET env var, demo-mode bypass with warning), 8 event-type → agent mappings (icsr.received, haq.received, comment.created, signal.validated, study.db_locked, submission.updated, capa.overdue, site.risk_elevated), optional cascade fan-out on signal.validated and study.db_locked when payload includes productId/submissionId, 202 Accepted response with agentRunId. All 8 gaps from the original autonomy gap analysis are now resolved.' },
  { version: '0.111.0', date: '2026-03-31', summary: '🤖 AUTONOMOUS AGENTS — 4 blocking gaps closed. (1) Gap 2 (CRITICAL): proactive/scan/route.ts now actually calls runAgent() for every triggered monitor via shared AGENT_REGISTRY — previously recorded scan stubs but never fired agents. (2) Gap 1: vercel.json added with hourly cron pointing at /api/agents/proactive/scan. (3) Gap 4: agent-store.ts fully rewritten as async with Prisma persistence — all 5 agent tables added to schema (AgentRun, AgentReviewRequest, AgentProactiveMonitor, AgentProactiveScan, AgentLearningFeedback); in-memory Maps retained as fast read cache + fallback when DATABASE_URL absent. (4) Gap 5 (write-back): agent-write-back.ts new module with concrete handlers for haq-response (writes responseText + status to HAQ), icsr-processing (writes narrative + MedDRA coding to safety case), document-resolution (resolves comment via authoring API), plus stubs for signal-intelligence/submission-assembly/quality-intelligence; called from agent-engine on autopilot path and from review/[id] on approval/modify. Supporting changes: agent-registry.ts shared agent map (eliminates duplication between trigger and scan routes); agent-api-fetch.ts shared fetch helper that throws in production when data is unavailable instead of silently returning mock data (Gap 3 — wired into Phase 1 agents); all store callers updated to await async functions; review/[id] approval path fires write-back.' },
  { version: '0.110.1', date: '2026-03-23', summary: '🐛 FIX: HAQ AI Assistant wired up — textarea and Ask AI button were decorative (no state, no onClick). Added aiPrompt useState, wired textarea value/onChange, wired button onClick to handleAIGenerate with disabled+spinner states, threaded aiPrompt into haq-rag API body as userPrompt. Cmd/Ctrl+Enter keyboard shortcut also fires generation.' },
  { version: '0.110.0', date: '2026-03-23', summary: '🧭 NAV CLEANUP — Sidebar restructure. (1) Regulatory sub-groups: 16 flat items organised into 5 scannable inline groups (Authoring & Docs, Submissions, Intelligence, Labeling, Strategy) via new groupLabel field on NavItem — each group renders a subtle label+rule separator before its first item. (2) Removed DEMO badge from R&D Connected nav item — production sidebar no longer signals demo mode. (3) Pinned items restructured: Home/R&D Connected/Agent Hub grouped as platform nav shortcuts (space-y-0.5 block, py-2 row height), My Tasks separated below with its own border-b divider, making the inbox function visually distinct from navigation destinations. (4) Default section states confirmed: Research/Clinical Development/Regulatory expanded; CMC/PV-Safety/Quality/Commercial/Intel collapsed. py-2.5 row padding trimmed to py-2 on pinned items for visual tightness.' },
  { version: '0.109.0', date: '2026-03-23', summary: '🤖 PHASE 5 — PORTFOLIO INTELLIGENCE + PROACTIVE MODE + LEARNING LAYER. (1) Portfolio Intelligence Agent (L4 Executive Orchestrator): portfolio-intelligence-agent.ts — reads across ALL 3 programmes and ALL 17 domain agent outputs; cross-programme milestone modelling; resource conflict detection (personnel/budget/lab-capacity/regulatory-slot); go/no-go scoring per programme (5 dimensions: Safety 30%, CMC 20%, Regulatory 25%, Clinical 15%, Resources 10%; composite → go/conditional-go/hold/no-go); board-ready executive reporting with investor narrative; cascade arbitration when competing cascades queue the same resource or regulatory slot. Risk floor: regulatory — portfolio reports NEVER autopilot. (2) AgentId expanded to 18 values (portfolio-intelligence added with violet P5 colour coding). Agent grid in hub shows P5 badge. Trigger panel DEFAULTS and selector cover all 18 agents. (3) Proactive Mode (src/lib/proactive-monitor-store.ts): 8 seeded monitors (5 threshold: signal PRR >3.0, HAQ overdue >2, enrollment velocity <4/week, CAPA effectiveness overdue, CMC stability OOS; 3 scheduled: weekly portfolio scan, daily reg commitment check, weekly literature scan); threshold evaluator with gt/lt/gte/lte comparators; scan history; toggle pause/resume; API routes: GET/POST /api/agents/proactive, GET/PATCH/DELETE /api/agents/proactive/[id], POST /api/agents/proactive/scan (evaluates all active monitors, fires agents, advances scheduled run times). (4) Agent Learning Layer (src/lib/learning-store.ts): 14 seeded feedback items spanning 6 agents; recordFeedback() from review decisions; computeCalibration() per-agent: approval rate, modification rate, rejection rate, risk agreement rate, avg draft quality 1-5, calibration status (well-calibrated/needs-review/recalibration-required), trend (improving/stable/degrading); computeLearningMetrics() portfolio view with 30-day learning curve; recalibrationQueue flags agents below thresholds; API routes: GET/POST /api/agents/learning. (5) Review queue wired to learning: decide() in ReviewCard now fire-and-forgets POST /api/agents/learning after every human review decision — outcome, risk agreement, improvement note all captured. (6) AgentHubScreen: 2 new tabs — Proactive (monitor grid with status/threshold/schedule, scan-now button, recent scan history) and Learning (calibration dashboard per agent with expandable detail, recalibration queue alert, recent feedback feed, quality/approval bars). Phase 5 seed run (run-p5-001) + review (rev-p5-001) for portfolio intelligence agent with full board report demo data.' },
  { version: '0.108.0', date: '2026-03-23', summary: '🤖 PHASE 4 — ADVANCED ROLES + IND→NDA AUTONOMOUS CHAIN. (1) Four new domain agents: medical-monitor-agent.ts (Virtual Medical Monitor — aggregates cohort safety data, evaluates stopping rules against predefined DSMB charter criteria, reads SAE narratives, prepares structured DSMB brief — ALL outputs mandatory human-in-loop, minimum REGULATORY risk level enforced), biostatistician-agent.ts (Virtual Biostatistician — SAP compliance check, TLF completeness audit with unplanned analysis detection, ICH E9(R1) estimand framework validation including intercurrent event strategy consistency, multiplicity adjustment verification), medical-affairs-agent.ts (Virtual MSL/Medical Affairs — AdPromo compliance check with off-label and fair-balance violation detection, medical information response drafting grounded strictly in approved label, publication review), lead-optimisation-agent.ts (Virtual Medicinal Chemist — reads lead series + ELN experiments, mines SAR patterns, scores ADMET profile with structural alert detection, ranks compounds against TPP, generates development candidate nomination package). (2) IND→NDA autonomous chain (ind-nda-chain.ts): 4 stage gates spanning full development: Gate 1 Candidate Nomination (Lead Optimisation → Reg Strategy IND pathway), Gate 2 IND Readiness (Nonclinical Package ICH M3(R2) → Reg Strategy filing plan), Gate 3 Phase 3 Site Readiness (Site Monitoring → Signal Intelligence → Study Operations go/no-go), Gate 4 NDA Assembly (Biostatistician DB lock readiness → Study Ops → Document Resolution CSR → Signal Intel final safety → Quality Intelligence pre-submission QC → Submission Assembly final score). (3) cascade/route.ts expanded to 7 total cascades (3 Phase 3 + 4 IND-NDA gates) with rich demo mode step sequences per cascade type. (4) trigger/route.ts: all 17 agents now registered. (5) types/agents.ts: AgentId expanded to 17 values with proper names, all Phase 3 agent files updated to use their correct AgentId (no more slot-sharing). (6) AgentHubScreen: 17-agent grid with P1/P2/P3/P4 colour coding (purple/blue/teal/amber), cascade panel expanded to 7 cascades with green/amber colour variants, trigger panel DEFAULTS and selector cover all 17 agents.' },
  { version: '0.107.0', date: '2026-03-23', summary: '🤖 PHASE 3 — FULL COVERAGE AGENTS + CROSS-DOMAIN CASCADES. (1) Six new domain agent files: nonclinical-package-agent.ts (ICH M3(R2) package completeness, ALCOA+ data integrity, GLP study assessment — virtual GLP Study Director), research-intelligence-agent.ts (literature triage, competitive threat assessment, target validation, programme viability — virtual Discovery Scientist), study-operations-agent.ts (CTMS study health, site activation bottlenecks, enrollment forecasting with ICH Q1E velocity model — virtual Study/CRO Manager), labeling-management-agent.ts (safety signal label impact, SPL/SmPC section drafting, cross-market consistency, variation type classification — virtual Labeling Specialist), cmc-lifecycle-agent.ts (stability trending with ICH Q1E linear regression, shelf life projection, change control regulatory classification, Module 3 updates — virtual CMC Scientist), cascade-orchestrator.ts (server-side multi-agent cascade engine: 3 cross-domain workflows). (2) Three autonomous cascades: signal-to-label (Signal Intelligence validates → Labeling Management drafts SmPC change → Quality Intelligence initiates CAPA — triggered by signal validation), db-lock-to-submission (Study Operations confirms DB lock → Document Resolution processes CSR comments → Submission Assembly rescores readiness — triggered by CTMS DB lock event), haq-cross-functional (HAQ Response classifies → Signal Intelligence pulls safety context if discipline is safety/clinical → Submission Assembly flags affected slot — triggered by new HAQ receipt). (3) POST/GET /api/agents/cascade route: fires any cascade by type, demo mode returns mock step sequence when no API key. (4) AgentHubScreen: Cascades tab added showing 3 cascade cards with P3 badge, step sequence visualisation, param inputs, fire button, step-by-step result display. tabs expanded to feed/review/cascades/trigger.' },
  { version: '0.106.0', date: '2026-03-23', summary: '🤖 PHASE 2 AGENTS — Five domain agents completing the core R&D persona coverage. (1) site-monitoring-agent.ts: Virtual CRA — reads CTMS site record + TMF gaps + protocol deviations, scores risk across enrollment/data-quality/compliance/adherence dimensions, recommends monitoring action (central/enhanced/triggered/for-cause visit). ICH E6(R3) risk-based monitoring principles. (2) submission-assembly-agent.ts: Virtual Submission Manager — reads submission + lifecycle + document slot completeness, computes readiness score 0-100, identifies blocking issues (missing approvals, placeholders, QC failures), generates ordered remediation steps. ICH M4 CTD, FDA 21 CFR 314/601. (3) signal-intelligence-agent.ts: Virtual Safety Physician — reads signal disproportionality stats (PRR/ROR/EBGM) + supporting ICSRs, performs EMA PrOACT-URL benefit-risk assessment, validates signal, recommends label section update and PSUR flagging. ICH E2E, ICH E2C(R2), CIOMS VIII. (4) quality-intelligence-agent.ts: Virtual QA Manager — reads CAPA + searches related quality events for systemic pattern detection, checks effectiveness check compliance, scores CAPA quality, escalates to senior management when pattern detected. ICH Q10, ICH Q9(R1). (5) reg-strategy-agent.ts: Virtual Reg Affairs Director — reads product regulatory status + commitment register + intelligence alerts, synthesises programme health, generates immediate actions and strategic recommendations. PDUFA commitments, EMA Day 120/180, breakthrough therapy obligations. Infrastructure: trigger/route.ts expanded to 8-agent registry. agent-store.ts: 3 Phase 2 seed runs (site monitoring HIGH risk, signal validated, CAPA systemic pattern) + 3 Phase 2 review requests with rich demo data. AgentHubScreen: agent grid updated to 4-column showing all 8 agents (P1 purple, P2 blue), trigger panel DEFAULTS and agent selector expanded to all 8.' },
  { version: '0.105.1', date: '2026-03-23', summary: '🔍 AGENT TRANSPARENCY — AgentHubScreen rewrite: full tool output visibility and entity context throughout. (1) Run feed: collapsed row now shows the actual content the agent read (evidence summary inline, e.g. comment text, HAQ question, ICSR event description) via Database icon below entity ID. Expanded run shows "What the agent read and did" section with ToolCallTrace component — each tool call is an expandable row showing input + full output, colour-coded by type (blue=reads, purple=search, teal=writes, amber=decisions). Risk rationale displayed prominently. (2) Review queue: ReviewCard now fetches the linked agent run on expand and shows "Source data — what the agent read" section (evidence-only tool calls) ABOVE the proposed action. "Why your review is needed" rationale box between source data and proposed action. Proposed action section uses human-readable field labels (FIELD_LABELS map). (3) ToolCallTrace component: shared between run feed and review cards, supports evidenceOnly=true mode for review queue. Per-tool smart formatters extract the most important output fields for each tool type (read_comment, read_haq, read_icsr_case, search_precedent, suggest_meddra_coding, etc). (4) Trigger panel: added entity ID hints per agent, improved result display shows risk level and autopilot/escalation outcome.' },
  { version: '0.105.0', date: '2026-03-23', summary: '🤖 PHASE 1 AGENTS — Multi-agent R&D operations system. (1) Agent infrastructure: src/types/agents.ts (AgentRun, AgentReviewRequest, risk levels, tool contracts), src/lib/agent-store.ts (server-side in-memory store with seed data, stats), src/lib/agent-engine.ts (Claude tool-use loop, risk routing, audit trail write-through). (2) Three Phase 1 agents: document-resolution-agent.ts (comment classification → editorial autopilot / regulatory escalation), haq-response-agent.ts (HAQ triage, RAG precedent search, response outline drafting), icsr-processing-agent.ts (MedDRA coding, ICH E2B narrative, regulatory clock). (3) API routes: GET/POST /api/agents/runs, GET /api/agents/runs/[id], POST /api/agents/trigger (all 3 agents, demo-mode fallback when no API key), GET /api/agents/review, GET+POST /api/agents/review/[id] (approve/reject/modify decisions with audit entry). (4) AgentHubScreen: live run feed with expandable tool call detail, human review queue with decision UI, manual trigger panel, stats strip (total runs, autopilot rate, pending reviews, avg duration). (5) Wiring: agent-hub ModuleId added to core.ts + module-registry.ts + RBAC ROUTE_RESOURCE_MAP; dynamic import in page.tsx; Bot icon + Agent Hub button in Sidebar (purple, NEW badge); noFilterBarModules updated.' },
  { version: '0.104.2', date: '2026-03-23', summary: '🎨 MOBILE + LIGHT MODE FIXES — RDConnectedScreen: (1) Light mode: text-white headings → text-text-primary; text-blue/green-300 badges → text-blue/green-600 (dark: variants preserved); scenario accent colors shifted to -700 dark:-400 for readable contrast on white backgrounds; bg-[var(--bg-primary)] → bg-surface; idle step card labels promoted from text-text-muted to text-text-secondary. (2) Mobile layout: 3-column layout converted to flex-col md:flex-row; left scenario panel capped at max-h-36 on mobile with horizontal scroll strip; right Business Case panel hidden on mobile (md:flex); Pipeline Scenarios heading hidden on mobile; step area padding 3 on mobile / 6 on md+. (3) iOS Safari: h-screen supplemented with [height:100dvh] on root container to fix dynamic viewport height jitter on scroll. (4) main element gets min-h-0 to prevent flex children overflowing their container on mobile. (5) Mobile landscape nav fix: useIsMobile/useIsTablet/useBreakpoint in useResponsive.ts now use max-height:599px guard to correctly classify phones in landscape (short viewport) as mobile rather than tablet — prevents icon-only sidebar appearing on phone rotation.' },
  { version: '0.104.1', date: '2026-03-23', summary: '🔐 RBAC FIX — ROUTE_RESOURCE_MAP expanded from 11 to 35 routes. /api/ai, /api/authoring, /api/tmf, /api/ctms, /api/qms, /api/eln, /api/glp, /api/research, /api/usdm, /api/idmp, /api/spl, /api/stability, /api/signatures, /api/gateways, /api/cross-module, /api/collaboration, /api/smart-submissions, /api/rag, /api/adam, /api/define-xml, /api/cdisc-terminology, /api/haq all added. Previously middleware skipped these routes (no ROUTE_RESOURCE_MAP entry) so user headers were never set — every requireAuth() call in these routes returned 401. Removed stale PUBLIC_ROUTES exceptions (/api/products, /api/documents, /api/haqs, /api/submissions, /api/cross-module) that were letting unauthenticated requests through. Health/metrics/docs remain public. Fixes AI Response Generation Authentication required error on HAQ screen.' },
  { version: '0.104.0', date: '2026-03-23', summary: '✅ PHASE 2 TASK 2.5 — PERSISTENCE VERIFICATION. (1) GET /api/health/db: new diagnostic endpoint — pings DATABASE_URL via SELECT 1, reports latency, counts rows in 4 core tables (haqs, tmf_artifacts, submission_lifecycles, authoring_documents), returns gate 2.5 PASS/FAIL in response body. Returns 503 if DATABASE_URL unset or unreachable, 207 if connected but a table query errors, 200 if all clear. (2) haqs/route.ts GET: removed 58-line dead Supabase-JS-client block (if (false && isSupabaseConfigured())) — GET now goes directly to Prisma → Supabase Postgres with no dead branches. Response source label updated from prisma to database. (3) haqs/route.ts POST fallback log: Supabase not configured message reworded so it does not match the 2.5 gate grep pattern — it only fires when NEXT_PUBLIC_SUPABASE_URL is absent (demo mode), not when DATABASE_URL is set. Gate check: GET /api/health/db → 200; create a HAQ, restart server, HAQ still exists; grep Supabase not configured logs: zero results on configured deploy.' },
  { version: '0.103.0', date: '2026-03-23', summary: '🔐 PHASE 2 TASK 2.4 — TENANT ID WIRING. Replaced 54 hardcoded tenantId: \'default\' occurrences across 16 API route files with dynamic JWT-derived tenantId: getRequestUser(request)?.tenantId ?? DEMO_TENANT_ID. Files updated: TMF (artifacts, gaps, auto-link, inspection-report, zones), Authoring (documents, documents/[id], sections, sections/[id], templates, templates/[id], templates/[id]/versions), Submissions (lifecycle, lifecycle/[id], lifecycle/tasks, lifecycle/tasks/[id]). All 16 files updated to import getRequestUser and DEMO_TENANT_ID from @/lib/api-auth. zones/route.ts received the api-auth import for the first time (previously had no auth imports). DEMO_TENANT_ID fallback preserves existing demo session behaviour — routes resolve to the seeded demo tenant UUID when no JWT tenant claim is present. grep \'tenantId.*default\' src/app/api: zero results. Roadmap gate 2.4 complete.' },
  { version: '0.102.0', date: '2026-03-23', summary: '🔒 PHASE 2 TASK 2.3 — RLS POLICIES. prisma/rls-policies.sql: idempotent SQL migration enabling Row Level Security on all 36 tables (tenants root + 35 tenant-scoped). auth.tenant_id() helper function reads claim from app_metadata.tenant_id (Supabase admin API path) with fallback to top-level tenant_id claim. tenants table: ENABLE RLS + FORCE RLS + single SELECT policy (id = auth.tenant_id()). All 35 tenant-scoped tables: ENABLE RLS + FORCE RLS + 4 policies (select/insert/update/delete) each enforcing tenant_id = auth.tenant_id(). service_role (used by all Prisma/server-side routes) bypasses RLS by Supabase default — zero impact on existing API routes. authenticated role enforced on every policy; anon role has no policies (zero access). prisma/apply-rls.ts: ts-node script that connects via DIRECT_URL, splits SQL into statements, applies each with error isolation, prints progress + summary. npm script apply:rls added to package.json. Verification queries included at bottom of SQL file: pg_tables check (expect rowsecurity=true on all 36) + pg_policies count (expect 141 total: 1 on tenants + 140 on 35 tables). To apply: copy prisma/rls-policies.sql into Supabase SQL Editor and Run, or npm run apply:rls with DIRECT_URL set.' },
  { version: '0.101.0', date: '2026-03-23', summary: '🗄️ PHASE 2 DATABASE SPRINT — Task 2.1 complete. (1) prisma/schema.prisma: full 33-model schema covering all tables accessed by API routes — Tenant, User, Product, Application, Study, Submission, SubmissionDocument, SubmissionSequence, SubmissionLifecycle, LifecycleEvent, ArchivalTask, HAQ, HAQResponseDraft, SafetyReport, Document, AuditLog, AuthoringDocument, AuthoringSection, AuthoringHistory, AuthoringContentBlock, AuthoringTemplate, AuthoringTemplateVersion, AuthoringTemplateChangelog, TemplateApprovalRequest, TemplateApprovalHistory, CAPA, Deviation, TMFArtifact, TMFGap, TMFAutoLinkEvent, TMFCompletenessReport, CTMSSite, CTMSSubject. UUID PKs throughout; tenantId on every tenant-scoped table; @@index on tenantId + common filter columns; explicit @@map snake_case table names. (2) prisma/seed.ts: demo tenant (00000000-…-0001), 5 users (admin/ra/safety/clinical/writer all with Ligature2026!), 2 products (Nexavant/CardioShield), 1 application (NDA-215847), 1 study (LIG-2847-301), 2 HAQs (IR-2026-001 / IR-2026-002), 1 safety report, 1 audit seed entry. (3) .env.example: all env vars documented with Supabase setup checklist. (4) src/lib/api-auth.ts: tenantId added to RequestUser; DEMO_TENANT_ID constant exported; getRequestUser reads x-tenant-id header (fallback to DEMO_TENANT_ID). (5) src/middleware.ts: JWTPayload.tenantId added; x-tenant-id header forwarded from JWT claim on every authenticated request. (6) src/lib/jwt.ts: TokenPayload.tenantId added; createToken() accepts and embeds tenantId claim. (7) src/app/api/auth/login/route.ts: demo mode embeds demo tenant UUID; production mode reads result.user.tenantId. (8) src/lib/auth-db.ts: AuthUser.tenantId added; findUserByEmail select includes tenantId. (9) next.config.js: explicit DATABASE_URL + DIRECT_URL env passthrough for Vercel build. Tasks 2.2–2.6 follow.' },
  { version: '0.100.0', date: '2026-03-23', summary: '🔒 PHASE 1 COMPLETE — Production security hardening. (1) JWT_SECRET fallback removed from all 3 files: src/middleware.ts, src/lib/audit-middleware.ts, src/config/auth-config.ts now throw on startup if JWT_SECRET env var is absent — hardcoded ligature-dev-secret eliminated. (2) @ts-nocheck sweep complete: all 36 remaining files cleaned (audit routes ×5, auth routes ×3, docuvera integration ×4, notification-channels-service, app layout/page/error/global-error, review layout/page, config/version, spike pages ×2, demo/template-browser, health routes ×3, tmf/zones route, users/invite routes, docs route, metrics route, compile/download route, errors route, cron/citations route, useResearchStore.ts.bak). @types/node installed — resolves 5 Buffer type errors in src/types/track-changes.ts. (3) next.config.js: eslint.ignoreDuringBuilds and typescript.ignoreBuildErrors removed — build now enforces TypeScript and ESLint on every push. Phase 1 gate: grep ligature-dev-secret src → 0 results; tsc --noEmit → 0 errors; npm run build → clean.' },
    { version: '0.99.9', date: '2026-03-23', summary: '🐛 UX FIXES — (1) R&D Connected idle state: replaced empty-void center panel with full step cards in pending state, header row with scenario name/stats, and two Run Pipeline CTAs (top and bottom). (2) HAQ Predicted Questions: Prepare Response button now navigates to Questions view filtered by matching discipline and auto-selects the best open HAQ. (3) useThemeStore template literal fix: escaped backtick on line 685 corrected.' },
    { version: '0.99.8', date: '2026-03-23', summary: '🎨 FOUC FIX + THEME SYSTEM OVERHAUL — (1) Anti-FOUC blocking script in layout.tsx reads persisted theme from localStorage and stamps the correct CSS class on <html> before the first paint, eliminating the dark→light flash that appeared on every page load. (2) globals.css restructured: :root now defaults to light-mode tokens; .theme-dark and .theme-high-contrast apply overrides via CSS class, meaning the correct colours are available before any JS runs. (3) applyThemeToDOM() now updates --status-* semantic tokens (previously missing — status badges used wrong colours in light mode). (4) Global CSS transition rule scoped to html[data-theme-ready] * — transitions are suppressed during the initial render and unlocked after the first paint, so the FOUC fix never produces a visible animation. (5) onRehydrateStorage switched from setTimeout(0) to requestAnimationFrame for tighter timing. (6) Pharma/custom theme body backgrounds now correctly override the CSS default.' },
  { version: '0.99.7', date: '2026-03-23', summary: '🔧 TS-NOCHECK CLEANUP WAVE 5 COMPLETE — @ts-nocheck removed from all remaining components (368 files). tsc --noEmit: zero errors. Fixes across components/: (1) Set/Map iteration → Array.from() in 23 files; (2) TS2304 cannot-find-name: missing state vars added to SignalDetectionDashboard, CTMSDeviationMonitoring, CTMSScreen, TMFScreen; (3) TS2339 property-does-not-exist: 16 interface additions across Cascade types, AuthoringDocument, ELNDataEntry, SignalSummary, RolePermissionsInput, HAQHistoryEvent, USDMObjective, ActivationProjection, SubmissionMilestone, ResponseDraftStatus; (4) TS2345 argument-type: 24 call sites cast to any or interfaces widened (QMSCreateModals all 4 modals, QMSScreen, ResearchScreen, CTMSScreen, AdminScreen, CRUDModals, CreateModals, SubmissionCommandCenter); (5) TS2322 type mismatches: PublishingPhase/Status extended, ICSRStatus extended, all QMS form data interfaces widened to string, TMFScreen Record casts, LifecycleManagementPanel onChange, SubmissionCommandCenter wizard handler; (6) TS2739/TS2740: ModuleType records in 4 cross-module components filled (document-control/qms/audit/executive), CrossModuleEventType extended with 8 demo/safety events and all component records updated, Breadcrumb+ReturnBreadcrumb filled with 20 missing ModuleId entries, E2EPublishingDashboard and SubmissionCommandCenter PHASE_CONFIG filled; (7) Misc: duplicate ValidationCategory renamed, TS2553/TS2554/TS2538/TS2503/TS2561/TS2300 all resolved.' },
  { version: '0.99.6', date: '2026-03-23', summary: '🔧 TS-NOCHECK CLEANUP WAVE 5 (partial) — @ts-nocheck removed from all 75 src/data/ files and 368 src/components/ files. Real errors fixed: (1) data/: global-submission-plan-data added us_reg to OWNERS + createdBy to demoUQPlan; ectd-document-templates TemplateSection/DocumentTemplate fields made optional + added category/aiAssisted/aiFeatures/lastUpdated/moduleSection/ectdLocation/guidance; m3-complete-templates import path fixed to local ectd-document-templates; regulatory-data + study-startup-data Set spread → Array.from(); module3-ectd-granular-templates complexity added to DocumentTemplate type. (2) components/: 23 Set/Map iteration errors fixed (Array.from pattern); 7 TS2304 cannot-find-name: selectedSignal added to SignalDetectionDashboard, viewMode/showScheduleModal to CTMSDeviationMonitoring first component, X to CTMSScreen imports, Archive to TMFScreen imports; 29 TS2339 property-does-not-exist: err cast to Error, automated/triggerLabel/triggerType added to Cascade types, type/completionPercentage to AuthoringDocument, toast destructuring fixed in ELNScreen, parameter to ELNDataEntry, ror to SignalSummary, permissions to RolePermissionsInput, persona.name → firstName+lastName, flag cast in RegulatoryIntelligenceScreen, description to USDMObjective (2 files), source to HAQHistoryEvent (3 files), description to SubmissionMilestone, cumulativeActivated/isProjected to ActivationProjection, enrollment.planned logic fix in StudyStartupOrchestrator; 3 TS2551 typos fixed (sourceUrl→source, activeModule→selection.activeModule); TS2561 scheduledStartDate→scheduledStart; TS2820 haq-response→response; TS2554 useRef initialized with null; TS2503 JSX.Element→React.JSX.Element; TS2538 unknown cast to string.' },
  { version: '0.99.5', date: '2026-03-22', summary: '🔧 TS-NOCHECK CLEANUP WAVE 4 — @ts-nocheck removed from all 175 files in src/services/ (174) and src/services/database/ (1). 350+ real errors fixed across 4 sessions: (1) es5 Set/Map iteration: 200+ for...of Set/Map/RegExpIterator patterns + [...new Set()] spreads converted to Array.from() across 80+ service files; (2) Import path fixes: createSupabaseRouteClient moved from @/lib/supabase/config to correct @/lib/supabase/server in 7 service files + document-service.ts; (3) Type fixes in services: ai-conflict-resolution-service narrowed string|AIResponseParsed union; cdisc-terminology logger args fixed; channel-templates RenderedTeamsMessage extends Omit<TeamsMessage,"potentialAction">; ctms-persistence StudySubject aliased from ClinicalSubject; eln-persistence entry.type→entryType, .verified→.verifiedBy; haq-correspondence-parser regex /s flag replaced with [\s\S]+?; MockStructuredContentService OutputFormat/DocumentType from docuvera/types, SearchResult shape fixed, as unknown as T casts; Notification interface extended with actionLabel/metadata/userId; notification-channels-service SlackBlockElement cast; push-notification Uint8Array cast + options as any; rag/prisma-pgvector $queryRawUnsafe cast; sync-conflict-resolution-service generic T return casts; track-changes/docx-builder OOXML duplicate type casts. (4) API route fixes: 21 route files request→req rename; ModuleType union extended with document-control/qms/audit/executive + all Record<ModuleType,...> objects updated in contracts.ts + useCrossModuleEventNotifications; Prisma enum imports (21 types across 21 files) replaced with type X=string aliases; CrossModuleEventType extended with 13 new event values; result.body cast to any in 7 validateRequest files; reduce() accumulator Record<string,unknown> params changed to any across 137 files; logger single/4-arg calls fixed; getSupabaseClient→createSupabaseRouteClient; PrismaDocumentAssignment cast; citationIds type widened; prisma.$transaction tx:any simplification. tsc --noEmit: zero errors.' },
  { version: '0.99.4', date: '2026-03-22', summary: '🔧 TS-NOCHECK CLEANUP WAVE 3 — @ts-nocheck removed from all 171 files in src/store/ (146) and src/stores/ (25). 19 files had real errors fixed: Set/Map es5 iteration converted to Array.from() across 7 files; useCrossModuleEventStore local ModuleId extended (nonclinical/ctms/archives/stability/biostats) fixing all 13 useGuidedScenarioStore errors; useNavigationStackStore MODULE_LABELS completed with 20 missing entries; studyIntelTypes domain import path fixed; useContentCollectionStore imports/field names/STATUS_MAP corrected; useMasterDataStore readonly never[] fixed; ICSRCase missing fields cast to any in usePharmacovigilanceStore and useSafetyRegBridge; useProductContextStore Set constructor cast; useResponseCampaign matches typed; useReviewerSimulation basedOn union corrected; useSPLStore SPLAuthor shape aligned + section codes uppercased + SPLText.format removed + effectiveTime object shape + ingredient fields corrected + SPLValidationResult.errors renamed to schemaErrors + DailyMedSearchResult shape fixed; useSnapshotStore data cast to any; useSubmissionLifecycleStore lifecycle.studyId cast. tsc --noEmit: zero errors in store/ and stores/. 606 files remain (services, components).' },
  { version: '0.99.3', date: '2026-03-22', summary: '🔧 TS-NOCHECK CLEANUP WAVE 2 — @ts-nocheck removed from all 106 files in src/hooks/ (57) and src/lib/ (49). 28 real errors surfaced and fixed: (1) 14× es5 Set/Map iteration — [...new Set()], for...of Set/Map, .entries() iterators all converted to Array.from() equivalents compatible with tsconfig target:es5; affects useCollapsedSections, useSubmissionLifecyclePersistence, audit-utils, date-calculation-engine, define-xml-generator, spl-xml-generator, monitoring (×5), spl-labeling-bridge (×2). (2) useAuthoringDocBridge + useQMSDocBridge: moduleId/moduleName/entityId -> moduleType/moduleEntityId/moduleEntityName per actual ModuleLink interface. (3) useTMFPersistence: qcDate/qcById/qcComments/approvedDate/approvedById are not TMFArtifact fields — replaced with qualityStatus:qc-passed/qc-failed + lastModifiedBy + updatedAt. (4) useUSDMPersistence: addStudy() -> createStudy(); studyVersion removed (not a USDMStudy field). (5) useQuickActions: SafetySignal[] cast through unknown before SafetySignalItem[]. (6) useCrossModuleEventNotifications: Set<CrossModuleEventType> annotation -> new Set<CrossModuleEventType>() generic. (7) audit-store: buildSeedEntry Omit widened to include id (required by computeChainHash). (8) auth-db: UserRole imported from @/lib/supabase/config not @prisma/client. (9) spl-labeling-bridge: SPLComponent.effectiveTime is {low?,high?} not string; LabelSection.lastModifiedAt is string not object — both corrected. (10) supabase/auth-service: createSupabaseRouteClient/createSupabaseAdminClient moved to import from server.ts (correct location). tsc --noEmit confirms zero errors in hooks/ and lib/. 892 files remain (stores, services, components).' },
  { version: '0.99.2', date: '2026-03-22', summary: '🔧 TS-NOCHECK CLEANUP WAVE 1 — First batch of @ts-nocheck removal from non-route files. (1) src/types/ (29 files): @ts-nocheck stripped from all type-definition files — core.ts, haq.ts, ectd.ts, authoring.ts, publishing.ts, citation.ts, and 23 others. Zero compiler errors after removal. (2) src/utils/ (3 files): ama-formatter.tsx, document-converters.ts, versionUtils.ts — all clean. (3) src/config/ (8 files): ai-authoring-config.ts, api-keys.ts, auth-config.ts, personas.ts, regional-config.ts, regulatory-citations.ts — all clean. (4) src/domains/ (20 contract files): all 20 domain contracts stripped — archives, authoring, cross-module, ctms, document-control, edc, eln, glp, haq, idmp, master-data, psmf, qms, safety, sample-management, study-intel, submission-readiness, submissions, tmf, usdm. (5) ONE real bug fixed: citation.ts:560 — spread of Set<string> incompatible with tsconfig target es5, changed to Array.from(new Set(...)). tsc --noEmit confirms zero errors. Total: 60 files cleaned, 1 real fix, 958 files remaining in hooks/lib/stores/store/services/components.' },
  { version: '0.99.0', date: '2026-03-22', summary: '⚡ REDIS CACHING + K6 PERFORMANCE SUITE — Phase 3 item 4 complete. (1) src/lib/cache/redis-cache.ts: unified cache module — lazy ioredis client (REDIS_URL optional, in-process Map fallback when absent), bounded fallback cache (512 entries, LRU eviction), cacheGet/cacheSet/cacheDel API, per-entry age tracking via shadow key, TTL constants (TTL_AGGREGATE_REPORT=3600s, TTL_HAQ_RAG_SIMILARITY=1800s, TTL_PIPELINES_GET=30s), deterministic key builders for all three hot paths. (2) POST /api/safety/aggregate-reports: cache check before Anthropic API call, X-Cache:HIT/MISS + X-Cache-Age response headers, fire-and-forget cacheSet after generation, x-cache-bypass:1 header for forced refresh. (3) POST /api/ai/haq-rag (similarity path): cache layer on generateDraft:false path only — streaming draft path intentionally not cached (SSE). (4) GET /api/cross-module/pipelines: cache layer on all three query types (active/templates/history), Supabase-not-configured fallback also cached. (5) tests/k6/helpers.js: shared BASE_URL, headers, SLA threshold objects, realistic payload factories (psurPayload, dsurPayload, haqSimPayload, haqCmcPayload). (6) tests/k6/aggregate-reports.k6.js: standalone PSUR/DSUR benchmark, setup() warm-up, custom aggregate_reports_duration Trend + cache_hit_rate Rate metrics, p(95)<800ms threshold. (7) tests/k6/haq-rag.k6.js: standalone HAQ similarity benchmark, Safety + CMC payload variants, haq_rag_duration Trend. (8) tests/k6/pipelines.k6.js: standalone pipelines GET benchmark, 3 query-type variants, 10 VUs. (9) tests/k6/performance-suite.k6.js: combined investor-grade suite — 3 concurrent k6 scenarios, setup() warms all 6 cache keys, teardown() summary, thresholds: all three p(95)<800ms + http_req_failed<1%. Run: k6 run tests/k6/performance-suite.k6.js. ioredis added to dependencies.' },
  { version: '0.98.0', date: '2026-03-22', summary: '🔗 AUDIT TRAIL INTEGRITY + EXPORT — Phase 3 item 2 complete. (1) src/lib/audit-store.ts: full SHA-256 chain rewrite — AuditEntry gains prevHash, chainHash (SHA-256(prevHash + canonicalJSON)), chainIndex fields; GENESIS_HASH (64 zero-hex) anchors the chain; verifyAuditChain() walks entries ordered by chainIndex, recomputes each link, returns { valid, totalEntries, verifiedAt, breakAt?, breakIndex? }; 4 richer seed entries with valid linked chain on cold start. (2) src/lib/audit-middleware.ts: new writeAudit(request, opts) fire-and-forget helper — resolves user identity from JWT cookie/header, writes chain-linked entry via addAuditEntry; never throws, never blocks response. (3) GET /api/audit/verify-chain: returns chain integrity result + algorithm/standard metadata (21 CFR Part 11 §11.10(e)). (4) GET /api/audit/export: full replacement — CSV now includes PrevHash + ChainHash columns; PDF rebuilt with pdf-lib: cover page (Ligature header, generation meta, chain status, Part 11 declaration box), entries table (alternating row shading, timestamp/user/action/module/details/hash-tail columns), chain integrity summary page (pass/fail banner, technical details, applicable regulations), page footers on every page. (5) writeAudit wired to 10 state-mutating routes: Safety workflow transitions, QMS CAPA create + update, Authoring document create, TMF artifact upload (mock + prisma paths), CTMS study create, CTMS site create, Document Control archive create, ELN experiment signing, Submissions lifecycle task update. (6) AuditTrailDashboard: useEffect on mount fetches /api/audit (liveEntries state) + calls runChainVerify() → /api/audit/verify-chain (chainDetails state); Exports subview replaced with live compliance export panel — chain integrity card with Re-verify button + status badge, format selector (PDF/CSV/JSON) with format description, download handler hitting /api/audit/export, live log preview table (20 rows, chainIndex + hash tail columns), historical export packages below.' },
  { version: '0.97.0', date: '2026-03-22', summary: '✍ 21 CFR PART 11 E-SIGNATURES — Phase 3 item 1 complete. (1) src/services/electronic-signature-service.ts: universal signature service — WorkflowEntityType (8 types), SignatureMeaning (9 meanings with §11.50(c) display text), createSignatureRecord(), computeHash() (SHA-256 via Web Crypto), verifySignatureChain() (chain integrity check, returns breaks array), getLastSignatureHash() for chain-index management; in-memory store with 3 seeded demo signatures. (2) src/components/ui/ElectronicSignatureModal.tsx: reusable 21 CFR Part 11 signature capture modal — displays entity label + action, signer identity from currentUser prop, meaning text (§11.50(c)), reason textarea (min 5 chars), optional password re-entry (§11.200(a)(1)), legal acknowledgment checkbox (§11.100), loading state, error display. (3) src/app/api/signatures/route.ts: POST records new signature — SHA-256 hash computation, chain index tracking, Supabase-first with in-memory fallback, audit log write-through (addAuditEntry); GET lists all or entity-filtered signatures with optional chain verify=true param. (4) SafetyScreen: "Approve for Submission" button on ICSR case-detail header (visible for new/processing cases), fires ElectronicSignatureModal, POSTs to /api/signatures, shows "✓ Signature applied" confirmation. (5) QMSScreen: "Close with Signature" button on CAPA detail panel (visible for pending-approval/investigation/implemented CAPAs), full modal flow, capaSignedIds Set tracks confirmed closures. (6) AuditTrailDashboard: live fetch from /api/signatures on mount, liveSignatures state replaces all mockElectronicSignatures runtime references, chain integrity badge (✓ verified / ⚠ break), loading spinner, "Live · 21 CFR Part 11" label. (7) docs/supabase-signatures-schema.sql: electronic_signatures table with immutability policies (no UPDATE/DELETE), verify_signature_chain() SQL function, indexes on entity/signer/timestamp/meaning.' },
  { version: '0.96.0', date: '2026-03-22', summary: '📐 USDM MATURATION — Full backend + two new tabs + AI protocol assist. (1) API routes: GET/POST /api/usdm/studies, GET/PUT/DELETE /api/usdm/studies/[id], GET/POST /api/usdm/tpp (Supabase-first + in-memory fallback with 5-claim seed: Efficacy×2, Safety, Convenience, PatientExperience). (2) POST /api/ai/usdm-protocol-assist: 3 actions — generate-objectives (ICH E9(R1) estimand framework, primary/secondary/exploratory with regulatory basis + rule-based fallback), assess-eligibility (ICH E6(R3)/E8(R1)/FDA 21CFR312/EMA GCP gap review, compliance score, IRB readiness), suggest-endpoints (CDISC SDTM v2.0 domain mapping, ADaM dataset refs, FDA/EMA regulatory acceptance + rule-based fallback). (3) src/hooks/useUSDMPersistence.ts: mount hydration from /api/usdm/studies + TPP seeding + design matrix mock seeding; online/offline detection; persistStudy/deleteStudy/persistTPP helpers. (4) StudyDesignScreen: 2 new tabs — Target Product Profile (claim swim lanes by MustHave/ShouldHave/NiceToHave priority, evidence status per claim, category progress bars) + Design Matrix (arms×epochs grid with CDISC DDF v3.0 badge, stat strip, element cells, arm detail cards); useUSDMTPPStore + useUSDMDesignMatrixStore wired for first time; Objectives tab: AI Suggest Endpoints + AI Generate Objectives buttons with inline result panels (CDISC SDTM domain badges, regulatory acceptance, fallback indicator); Loader2 added to imports.' },
  { version: '0.95.1', date: '2026-03-22', summary: '🎨 FAVICON + PWA ICON FIX — Replaced placeholder blue-L favicon with the real Ligature logo mark across all icon sizes. Generated from ligature-logo.png: favicon-16.png, favicon-32.png (transparent bg, browser tab), icon-192.png, icon-512.png (dark #0D1B2A bg, app icons), icon-maskable-192.png, icon-maskable-512.png (safe-zone padding for Android adaptive icons), apple-touch-icon.png (white bg, iOS home screen). Browser tab and PWA "Open in app" button now show the correct cursive Ligature mark.' },
  { version: '0.95.0', date: '2026-03-22', summary: '🔗 R&D CONNECTED SCREEN — Phase 3 investor demo centrepiece. New screen: src/components/screens/RDConnectedScreen.tsx — full-screen pipeline orchestrator with 7 selectable scenarios (lslv-to-submission, signal-to-label, lab-to-m4, haq-to-submission, deviation-to-capa, cmc-change-flow, study-setup). Self-contained execution engine: animated step-by-step progression with per-step elapsed timers, live progress bar, wall-clock ticker. Left panel: scenario selector cards with cycle reduction badges. Centre: live StepCard visualiser (pending→running→completed states, module-to-module flow arrows, narrative text per step). Right panel: business case — cycle compression hero (legacy vs Ligature days), impact metrics grid, step tracker, platform coverage stats. Post-run ImpactPanel: hero elapsed time, reduction %, impact grid, before/after timeline bar. Wired: ModuleId rd-connected added to core.ts; dynamic import in page.tsx; noFilterBarModules; module label map. Sidebar: R&D Connected pinned below Home with teal Workflow icon and DEMO badge. Workflow icon added to Sidebar imports.' },
  { version: '0.94.0', date: '2026-03-22', summary: '🔬 EDC + GLP INTEGRATION — Full Electronic Data Capture / LIMS integration layer for GLP nonclinical studies. (1) src/store/glpTypes.ts: GLPTab extended with edc-integration. (2) /api/glp/edc-link: GET+POST+DELETE CRUD for GLPEDCLink records — vendor (Medidata Rave, LabVantage, Oracle Clinical One, etc.), completeness, integrity flag count, 6-endpoint breakdown per link, 21 CFR 58 Subpart D CSV/audit-trail flags; Supabase-first + in-memory fallback with 3 seeded connections. (3) /api/glp/edc-sync: POST manual sync trigger — simulated EDC pull with per-endpoint record counts, integrity flag detection (out-of-range, audit-trail-gap, timestamp-anomaly, unauthorized-edit), returns GLPEDCSyncResult. (4) /api/ai/glp-data-integrity: 3 actions — analyze-integrity (ALCOA+ 9-dimension score, risk level, submission readiness, remediation plan, executive summary); assess-endpoint (single endpoint deep-dive); generate-capa (full CAPA with root cause, immediate/corrective/preventive actions, weekly implementation timeline); rule-based fallback on all 3 actions. (5) src/hooks/useGLPEDCPersistence.ts: mount hydration, persistLink/deleteLink/triggerSync, optimistic link state updates, online/offline detection. (6) src/components/glp/GLPEDCIntegrationTab.tsx: ConnectorCard with expandable endpoint breakdown, ALCOA+ radar SVG chart, SyncResultPanel with flag list, AIIntegrityPanel, AICAPAPanel, KPI strip (active connections, avg completeness, integrity flags, CSV validation gaps), regulatory reference sidebar (21 CFR 58.63/130/185/195, FDA DI Guidance 2018, OECD GLP CN No. 17). (7) GLPScreen: edc-integration tab wired (import + TABS + render). (8) docs/supabase-glp-edc-schema.sql: glp_edc_connections + glp_edc_sync_log (append-only per 21 CFR 58.195) with RLS, indexes, updated_at trigger.' },
  { version: '0.92.0', date: '2026-03-22', summary: '🔬 IND READINESS GAP ANALYSIS ENGINE — Phase 2 Session 4. New route POST /api/ai/ind-gap-analysis: 3 actions — analyze-gaps (full programme analysis: criticalGaps[] with section/priority/regulatoryBasis/recommendedAction/estimatedDaysToResolve, atRiskItems[], readinessProjection with projectedReadinessAtTarget/onTrack, regulatorySufficiency with fdaPhase1Eligible/blockers, actionPlan by week, executiveSummary); assess-section (deep CTD section assessment: sectionAssessment narrative, regulatoryStandard citation, currentDeficiencies[], remediationSteps[], riskIfSkipped, readinessScore); generate-timeline (critical-path timeline: feasibility, criticalPath[] with dependencies/isCritical, milestones[], resourceRequirements[], risks[], submissionReadyDate, contingencyDate). Regulatory knowledge base: ICH_M3_REQUIREMENTS map covering all 8 required CTD sections with FDA/ICH citation; CRITICAL_THRESHOLD 40%, AT_RISK_THRESHOLD 70%, DAYS_CRITICAL 60 constants; rule-based fallback scoring for no-API-key scenario. IND Readiness tab upgraded: (1) AI Gap Analysis button in header triggers analyze-gaps with linked preclinical studies; (2) gap results panel renders above the program grid — overallRisk badge, onTrack indicator, executiveSummary, criticalGaps cards with regulatory basis, action plan weeks; (3) per-section AI Assess button fires assess-section; inline assessment result shows sectionAssessment narrative, currentDeficiencies, regulatoryStandard citation.' },
  { version: '0.91.0', date: '2026-03-22', summary: '🤖 AI LITERATURE TRIAGE — /api/ai/research-intelligence. (1) New route POST /api/ai/research-intelligence: 4 actions — triage-alert (single alert: relevance score 0-100, competitive threat high/medium/low/none, regulatory impact, recommendedAction urgent-review/review/monitor/archive, keyInsights[], researchBrief, confidence); batch-triage (up to 10 alerts in one call, AI-ranked by relevance descending); competitive-analysis (landscape summary, top threats with urgency, opportunities, watchList, overallCompetitivePosition); trend-analysis (emergingThemes with momentum, scientificShifts, regulatoryTrends, competitiveMomentum, executiveSummary). All 4 actions rate-limited (standard 20/min via ai-rate-limiter). Rule-based fallback on all actions when no API key — keyword scoring against programContext fields. (2) ResearchScreen literature tab upgraded: AI Triage All button triggers batch-triage on filteredLiterature (up to 10); per-alert Triage button fires triage-alert with KRAS G12C/NSCLC/IND-enabling context; results displayed inline — action badge + score chip, re-triage refresh icon; expandable insight panel per triaged alert showing researchBrief, keyInsights[], competitiveThreat + regulatoryImpact indicators; updated relevance written back to persistence layer via persistAlert when AI changes the score.' },
  { version: '0.90.0', date: '2026-03-22', summary: '🔬 LEAD OPT + TRANSLATIONAL PERSISTENCE — Phase 2 Session 2. (1) useResearchStore expanded: 5 new slices — leadSeries[], candidateSelectionSummary, translationalStudies[], biomarkerPanels[], safetyTranslations[]; actions: addLeadSeries/updateLeadSeries/removeLeadSeries/setSeriesStatus/updateCandidateSelectionSummary, addTranslationalStudy/updateTranslationalStudy/removeTranslationalStudy, addBiomarkerPanel/updateBiomarkerPanel/removeBiomarkerPanel, updateSafetyTranslation; loadLeadOptData + loadTranslationalData seed functions (idempotent). (2) research-persistence-service.ts extended: fetchLeadSeries/upsertLeadSeries/deleteLeadSeries (JSON blob strategy — full LeadSeries including compounds array serialised as data_json); fetchTranslationalStudies/upsertTranslationalStudy/deleteTranslationalStudy (same blob pattern); seedLeadTranslationalIfEmpty() cold-start seed. (3) New API routes: GET+POST /api/research/lead-series (?productId filter), PUT+DELETE /api/research/lead-series/[id]; GET+POST /api/research/translational-studies (?productId filter), PUT+DELETE /api/research/translational-studies/[id]. (4) useResearchPersistence.ts extended: useLeadOptPersistence() hook — hydrates lead series from API on mount, graceful seed fallback, exposes persistLeadSeries/deleteLeadSeries; useTranslationalPersistence() hook — same pattern for translational studies. (5) ResearchScreen wired: useLeadOptPersistence + useTranslationalPersistence imported and called; 10 static helper function calls (getLeadSeriesByProduct, getTranslationalStudiesByProduct, getBiomarkerPanelByProduct, getSafetyTranslationsByProduct, getLeadOptStats, getTranslationalStats, candidateSelectionSummary.*) replaced with store selectors and inline computed values; handleSetSeriesStatus/handleUpdateLeadSeries/handleUpdateTranslationalStudy mutation handlers added with API persist call-through. (6) docs/supabase-research-schema.sql: DDL appended for research_lead_series and research_translational_studies tables (JSON blob + indexed scalar columns, RLS, updated_at triggers).' },
  { version: '0.89.0', date: '2026-03-22', summary: '🗄️ RESEARCH PERSISTENCE — Phase 2 Session 1. Core API routes + Supabase schema + useResearchPersistence hook. (1) src/services/research-persistence-service.ts: Supabase-backed CRUD for all three research entity types — fetchTargets/upsertTarget/deleteTarget, fetchStudies/upsertStudy/deleteStudy, fetchAlerts/upsertAlert/deleteAlert; full snake_case↔camelCase row mappers; module-level in-memory fallback store (survives navigation); seedMemoryIfEmpty() called on cold start from static research-data.ts seed. (2) API routes — GET+POST /api/research/targets, PUT+DELETE /api/research/targets/[id]; GET+POST /api/research/studies (productId/status/type filters), PUT+DELETE /api/research/studies/[id]; GET+POST /api/research/literature (productId/relevance/type/search filters), PUT+DELETE /api/research/literature/[id]. (3) src/hooks/useResearchPersistence.ts: mirrors useSafetyPersistence architecture — hydration on mount with 5s timeout + AbortController, parallel fetch of all three endpoints, API data wins over static seed, Zustand store force-updated via setState, graceful offline fallback to static seed data; exposes persistTarget/deleteTarget/persistStudy/deleteStudy/persistAlert/deleteAlert helpers; online/offline detection via window events. (4) ResearchScreen wired: hook imported, all 5 mutation handlers (handleCreateTarget, handleCreateStudy, handleUpdateTarget, handleUpdateStudy, handleDeleteTarget, handleDeleteStudy) now call persist/delete API fire-and-forget after optimistic store update. (5) docs/supabase-research-schema.sql: full DDL for research_targets, research_preclinical_studies, research_literature_alerts — RLS policies (authenticated read/write), set_updated_at trigger, indexes on product_id/stage/status/type/date.' },
  { version: '0.88.0', date: '2026-03-22', summary: '🗺️ GLOBAL SUBMISSION PLAN BACKEND + IDMP API — Phase 1 close-out. (1) POST /api/submissions/global-plan: replaces wizard setTimeout stub with real backend — ISO 11615 region gap analysis classifies all CTD sections as CPI (16 shared M2–M5 sections) vs regional-delta (9 market-specific M1 sections), computes per-region timeline (fileByDate, dataLockDate, reviewCompletionDate from agency lead-time table: FDA 365d, EMA 450d, PMDA 400d, NMPA 500d, HC 350d, TGA 300d, MHRA 350d), builds task seed (9 CPI core tasks + lead-market PI/RMP/cover/publish tasks + per-satellite SmPC/cover/compile tasks), persists to Supabase with in-memory fallback; GET /api/submissions/global-plan lists plans from Supabase or memory. GlobalSubmissionPlanWizard: handleSubmit wired to real fetch — success toast now shows CPI/delta section counts. (2) GET /api/idmp/products: serves 3 seed products (Nexavant/LIG-2847, CardioShield/LIG-3901, OncoTarget/LIG-5104) with ISO 11615 full field set including authorizations, MAH, therapeutic indications; Supabase-first with seed fallback. POST /api/idmp/products: ISO 11615 validation (6 required fields with standard codes), upserts to Supabase idmp_medicinal_products table. IDMPDashboard: useEffect now tries GET /api/idmp/products first, hydrates store from API products, falls back to loadMockData(); header shows Supabase/API/seed source indicator.' },
  { version: '0.87.0', date: '2026-03-22', summary: '✅ HAQ REVIEW WORKFLOW — REAL API WIRING (Phase 1, M1 gate). New hooks: useHAQReviews (src/hooks/useHAQReviews.ts) — fetches GET /api/haqs/[id]/reviews on mount, optimistic updates on assign/complete, mirrors all mutations to Zustand store, fires audit events on status transitions (under-review on first assign, in-progress on changes-requested, submitted on full 3-stage approval), graceful Zustand fallback when no Supabase. useHAQAuditTrail (src/hooks/useHAQAuditTrail.ts) — fetches GET /api/haqs/[id]/audit, merges with local store history by id dedup, sorts newest-first. HAQReviewPanel rewritten: role labels updated to Subject Matter Expert / Regulatory Lead / QA/Compliance; stage cards with sequential gate enforcement; feedback textarea on Request Changes (two-click: first click reveals textarea, second sends); auto-expand next stage on approval; all-approved banner; Workflow Rules explainer; Refresh button. HAQTimeline updated: pulls from useHAQAuditTrail, shows "Live audit trail (21 CFR Part 11 compliant)" badge when API data present, "verified" source tag per event, loading spinner, graceful fallback to local store events.' },
  { version: '0.86.0', date: '2026-03-22', summary: '🧪 PSUR/DSUR/PADER AGGREGATE REPORT API — Phase 1 Priority #1 complete. New route: POST /api/safety/aggregate-reports — consumes real ICSR seed data, filters cases by product+period, builds CaseStatistics (total/serious/expedited/SOC/causality breakdown), generates line listing rows, evaluates active signals with PRR/ROR, calls Anthropic claude-sonnet-4-6 for three ICH-narrative sections (signal evaluation, benefit-risk, conclusions), assembles ICH E2C(R2)/E2F-structured section tree (17 sections PSUR, 14 sections DSUR, 6 sections PADER), returns structured JSON. Graceful fallback: static narrative content when no API key configured. New component: AggregateReportGenerationPanel — 6-step progressive generation UI (validate→cases→stats→signals→AI→compile), 4-tab result viewer (ICH sections with AI badge, statistics with SOC bar chart, signal evaluation with PRR/ROR, line listing table), JSON download. SafetyScreen: Generate button on every periodic report row (Actions column), panel modal wired.' },
  { version: '0.85.1', date: '2026-03-22', summary: '📄 OPENAPI FULL COVERAGE — API documentation expanded from 39 routes (31%) to 127/127 routes (100%). New file: src/lib/openapi-extensions.ts — 88 missing paths across 18 new tag groups: AI Services (10 routes, all with rate-limit and 429 responses documented), Authoring (11), CTMS (6), Document Control (4), Documents extras (2), HAQ extras (3), Health & Metrics (4), QMS (4), RAG (3), Safety extras (5), Smart Submissions (2), Submissions lifecycle/compile (6), TMF (9), Users (3), and Miscellaneous/standards/monitoring (15). Merged into generateOpenAPISpec() via spread — existing 39-route paths object untouched. Also fixed Map iteration in ai-rate-limiter.ts (Array.from for tsconfig ES target compatibility).' },
  { version: '0.85.0', date: '2026-03-22', summary: '🔒 SECURITY & TYPE HARDENING — Two high-signal fixes addressing the top gaps from the v0.84.0 maturity assessment. (1) @ts-nocheck removed from src/lib/rbac.ts and src/config/module-registry.ts — both files compile clean with zero TypeScript errors; the suppressions were masking no actual issues and have been eliminated. These are the two files where type safety matters most (access control and commercial entitlements). (2) AI rate limiter: new src/lib/ai-rate-limiter.ts token-bucket implementation — per-user buckets keyed on JWT session user ID (IP fallback), automatic stale-bucket eviction, three presets (standard: 20/min burst-5, batch: 5/min burst-2, health: 60/min). All 9 AI POST routes patched: generate, haq-rag, safety-signal, safety-narrative, safety-intelligence, document-qc, section-generate, stream-spike, batch-section. 429 responses include Retry-After + X-RateLimit-* headers. Health route (GET-only) correctly excluded.' },
  { version: '0.84.0', date: '2026-03-21', summary: '🎨 STYLE SYSTEM HARDENING — Three pre-existing gaps closed, one regression fixed. (1) tailwind.config.js: all 22 design-system CSS variable tokens registered as Tailwind color utilities (bg-surface-elevated, text-text-primary, border-border, all 6 accent + 6 status colors). (2) Shadow tokens: shadow-modal / shadow-elevated / shadow-card defined in boxShadow extend — previously silently no-op in Modal.tsx, Card.tsx, Select.tsx. (3) tailwindcss-animate plugin installed + registered — animate-in / fade-in / zoom-in-95 / slide-in-from-* now produce real keyframe CSS in Modal.tsx, PWARegistration, PresenceAvatars, SafetySignalCascadeOverlay, TextSelectionPopover. (4) Raw color regressions from v0.82.0 theme migration: IDMPDashboard pipeline stage bars (bg-slate-600/500 → bg-border/bg-status-neutral), status badge (bg-slate-500/20 → bg-surface-hover text-text-secondary), two disabled button states (bg-slate-600 → bg-status-neutral cursor-not-allowed); ContinuousSubmissionDashboard unknown status dot (bg-gray-400 → bg-status-neutral).' },
  { version: '0.83.0', date: '2026-03-21', summary: '📊 USER BEHAVIOR ANALYTICS (Option B) — Platform-wide behavioral event bus built on the existing templateUsageTrackingService pattern. (1) usage-event-service.ts: singleton service with 6 event categories (module_visit, feature_action, ai_invocation, search, export, error_encountered), rich query layer (DAU/WAU, top modules with 7-day sparkline trends, top features, user journey transitions, daily event counts, avg session duration), localStorage persistence capped at 5000 events, seeds 30 days of realistic mock history on first load, exportCSV() method. (2) useTrackEvent.ts hook: one-liner event firing for any component — trackModule/trackAction/trackAI auto-reads user identity from auth store. (3) page.tsx: handleModuleChange wired to fire module_visit on every navigation. (4) PlatformAnalyticsDashboard: new "User Behavior" tab — hero stats row (events, sessions, DAU, WAU, avg session), Most-Used Modules bar chart with 7-day sparklines, Top Feature Actions ranked list, Daily Activity histogram with hover tooltips, Common User Journeys transition list, Recent Events live feed, Export CSV button.' },
  { version: '0.82.0', date: '2026-03-21', summary: '🎨 DESIGN SYSTEM + UI DEPTH — (1) IDMPDashboard: full port off gray-50/white theme to design system tokens — ScreenHeader with Database icon + sync action, all bg-gray/text-gray → bg-surface-*/text-text-* tokens, status badges use bg-*/20 text-*-400 pattern, rounded-xl cards, teal-500/30 standards compliance panel. (2) ContinuousSubmissionDashboard enrichment: HealthAuthority Connections panel (FDA/EMA/PMDA/HC with live/degraded/offline status dots, pending items, ack count, latency ms), Recent Submissions table (submission ID, product, module, target, timestamp, status badge with pulse dots). (3) PortfolioStrategyView depth: filter toolbar (product dropdown + type dropdown + zoom +/- buttons), two-column layout with right sidebar showing At-Risk/Delayed alerts panel + Next 90 Days upcoming milestones list (days-until badge, product label, click-to-detail). (4) CommitmentsManager depth: filter toolbar with search input, status dropdown, type dropdown, result count, Kanban/List view mode toggle — all wired to existing state.' },
  { version: '0.81.0', date: '2026-03-21', summary: '🩺 COMPLIANCE TAB COMPLETION — Filled all 9 "Coming Soon" stubs across GCP, GMP, and AdPromo screens. GCPScreen: Training tab (ICH E6 per-person/site records, compliance bars, overdue filters, required courses reference), Essential Documents tab (ICH E6(R2) Table 1 checklist — 14 docs across Before/During/After, per-site on-file/missing/expired/pending matrix), Inspections tab (FDA/EMA/MHRA/PMDA tracker with readiness scores, finding categories, CAPA links, pre-inspection checklists). GMPScreen: Equipment tab (IQ/OQ/PQ qualification table, 30-day calibration/PM alerts, criticality ratings), Training tab (cGMP personnel records by area, SOP/regulatory/hygiene/equipment type filters). AdPromoScreen: References tab (annotated reference library with DOIs, claim counts, expiry tracking), History tab (completed review log with per-reviewer decisions, cycle times, revision rounds), Training tab (MLR team training records with certification expiry).' },
  { version: '0.81.0', date: '2026-03-21', summary: '🏥 FILL FUNCTIONAL GAPS — Replaced all 9 coming-soon tab stubs with full domain-accurate content. GCPScreen: Training tab (per-person/site ICH E6 records, compliance % by site, required courses registry), Essential Documents tab (ICH E6 §8 checklist with per-site on-file/missing/expired matrix), Inspections tab (FDA/EMA/MHRA/PMDA tracker, readiness scores, findings with CAPA links, pre-inspection checklists). GMPScreen: Equipment tab (IQ/OQ/PQ status table, calibration/PM due-date alerts, criticality ratings), Training tab (cGMP records by area, SOP/regulatory/hygiene type filters). AdPromoScreen: References tab (annotated reference library with claim links, DOIs, expiry dates), History tab (completed review log with per-reviewer decisions, cycle times, approval detail panel), Training tab (MLR team records with certification expiry tracking). Also fixed duplicate Archive import in GCPScreen and created missing tsconfig.json.' },
  { version: '0.80.11', date: '2026-02-27', summary: '🗄️ PERSISTENT AUDIT LOG — AuditLog Prisma model with timestamp/user/email/action/module/details/type fields + User FK. audit-store.ts rewritten: addAuditEntry() fires sync in-memory write + async DB persist; getAuditEntriesAsync()/getAuditCountAsync() read from DB first with memory fallback. GET /api/audit uses async DB-backed functions. AdminScreen overview panel fetches live audit data. Also: fixed TMF Archive icon crash (missing lucide-react import).' },
  { version: '0.80.0', date: '2026-02-26', summary: '🗄️ DATABASE PERSISTENCE — Prisma schema (prisma/schema.prisma) with User model: UserRole enum (ADMIN/REGULATORY_LEAD/MEDICAL_WRITER/REVIEWER/SAFETY_OFFICER/CLINICAL_OPS), UserStatus enum, full auth fields (passwordHash, failedLoginAttempts, lockedUntil, sessionToken, invitedBy/At/acceptedAt). Seed file (prisma/seed.ts) creates 5 default users. Invite route writes to DB (upsert) then falls back to in-memory. Accept route writes accepted user with bcrypt-hashed password to DB. GET /api/users reads from DB when available, falls back to hardcoded seed list. Setup: add DATABASE_URL + DIRECT_URL to Vercel, run npx prisma db push && npx prisma db seed, remove DEMO_MODE.' },
  { version: '0.79.1', date: '2026-02-26', summary: '🔒 PERSISTENT USERS — Server-side user store (src/lib/user-store.ts) replaces ephemeral React useState. Module-level Map survives navigation and page refreshes. GET /api/users endpoint returns full user list. Invite route registers pending user in store immediately. Accept route marks user active on onboarding. AdminScreen fetches users on mount with loading spinner; refreshes from server after each invite instead of patching local state.' },
  { version: '0.78.0', date: '2026-02-26', summary: '📧 REAL USER INVITES — /api/users/invite route with Resend email integration. Production: sends branded invite email with secure token link. Demo mode: returns invite link for manual sharing with clear in-UI explanation (no fake "email sent" message). AdminScreen invite button wired to real API with loading spinner and error handling. Add RESEND_API_KEY to Vercel env vars to enable email delivery.' },
  { version: '0.68.0', date: '2026-02-26', summary: '🚀 v0.68.0 — Demo deployment fully working: auto-auth, fixed infinite sync spinner, stable Demo badge in header.' },
  { version: '0.67.13', date: '2026-02-26', summary: '🔧 Demo mode auto-auth: session/login APIs bypass database when no DATABASE_URL is set. App loads instantly without login.' },
  { version: '0.67.12', date: '2026-02-25', summary: '🔧 Removed broken demo/publishing-enhancements page; error pages now show details in production; bumped SW cache version.' },
  { version: '0.67.11', date: '2026-02-25', summary: '🔧 FIX: Corrected lockfile integrity hash for next@15.5.7 (hash was for 15.5.11).' },
  { version: '0.67.10', date: '2026-02-25', summary: '🔧 FIX: Exclude lockfile from deploy — manually-edited integrity hash caused EINTEGRITY failure. Let Vercel regenerate clean lockfile.' },
  { version: '0.67.9', date: '2026-02-25', summary: '🔧 FIX: Corrected lockfile resolved URL for next to 15.5.7 (was pointing to 15.5.11.tgz), ensuring Vercel installs matching SWC.' },
  { version: '0.67.8', date: '2026-02-25', summary: '🔧 FIX: ActivityScreen missing closing div in main return; AdPromoScreen spurious </ScreenHeader> appended to end of file.' },
  { version: '0.67.7', date: '2026-02-25', summary: '🔧 BUILD FIX: Repaired 5 JSX structural errors from v0.51.0 ScreenHeader migration — ActivityScreen (extra div), AdPromoScreen (</div> → </ScreenHeader>), AuthoringScreen (missing fragment), HomeScreen (modal outside root div), SubmissionsHubScreen (extra div + </ScreenHeader>).' },
  { version: '0.67.6', date: '2026-02-25', summary: '🔧 BUILD FIX: Downgrade next to 15.5.7 — @next/swc-linux-x64-gnu@15.5.11 does not exist on npm. Pin both next and all @next/swc-* overrides to 15.5.7.' },
  { version: '0.67.5', date: '2026-02-25', summary: '🔧 BUILD FIX: Added npm overrides to force all @next/swc-* to 15.5.11, preventing Vercel build cache from restoring stale 15.5.7 SWC binaries.' },
  { version: '0.67.4', date: '2026-02-25', summary: '🔧 BUILD FIX: Reconstructed corrupted RegStrategyScreen.tsx (missing header). Removed package-lock.json from deploy — Vercel regenerates clean, resolving next@15.5.11 + @next/swc@15.5.11 match.' },
  { version: '0.67.3', date: '2026-02-25', summary: '🔧 BUILD FIX: Pinned next to 15.5.7 to match swc binaries locked in package-lock.json. Removed duplicate /> in GlobalSubmissionPlanScreen.' },
  { version: '0.67.2', date: '2026-02-25', summary: '🔧 FIX: Repaired mangled className template literal on Baseline button in GlobalSubmissionPlanScreen (caused Vercel build failure).' },
  { version: '0.67.1', date: '2026-02-25', summary: '🔧 BUILD FIX: Removed output:standalone (Vercel conflict), added outputFileTracingRoot (silences multiple-lockfile warning). No functional changes.' },
  { version: '0.67.0', date: '2026-02-25', summary: '📸 BASELINE + EXPORT — LIG-013 COMPLETE: Set Baseline captures current plannedStart/Finish per task; baselineCapturedAt/By on plan; deviation column (+/-d) in plan grid; baseline ghost bar layer in GanttPanel (dashed outline behind live bar). BaselineModal: confirm/replace/clear UX with task count, existing-baseline warning. LIG-014 COMPLETE: PlanExportPanel wired — Export button in toolbar opens print-optimised overlay with plan summary, stats grid, task table; deviation column shown when baseline exists; browser print-to-PDF; respects current filters. New files: BaselineModal.tsx. Store: setBaseline, clearBaseline actions in global-plan-store.' },
  { version: '0.66.0', date: '2026-02-25', summary: '📅 GANTT CHART — LIG-012 COMPLETE: GanttPanel full-screen overlay with synced frozen label column + horizontal/vertical scroll. Three zoom presets: Week (22px/d) · Month (6px/d) · Quarter (2.2px/d). Task bars colour-coded by module section (M1=violet, M2=cyan, M3=amber, M4=pink, M5=emerald). Critical path bars: amber border + ⚡ badge. Status fill: complete=emerald, in-progress=section colour, not-started=muted. Overdue tasks: red left-cap. Actual finish marker line for complete tasks. Today line: red dashed vertical with TODAY label. Trigger milestone: violet diamond + label. Dependency bezier curves with arrowheads (toggleable). Section group separators in both label column and SVG. Click task → detail panel (dates, status, overdue callout, critical/lock/calc badges, predecessors, successors, owner, notes). Hide Inactive toggle. Jump to Today button auto-centres view. Zoom change auto-scrolls to today. Legend bar. Wire: Gantt button in GlobalSubmissionPlanScreen toolbar, showGantt state, GanttPanel import.' },
  { version: '0.65.0', date: '2026-02-25', summary: '📅 DELAY REPORTING + SUCCESSORS + DATE LOCK — LIG-017 COMPLETE: getDaysOver helper, red overdue badge, Delayed stat card, Delayed Only filter. LIG-011 COMPLETE: buildSuccessorMap, bidirectional successor display in cyan in expanded row. LIG-018 COMPLETE: inline date edit pickers, Lock dates toggle, isDateLocked respected by recalculate engine. Store: refreshDaysOver, updateTaskDates, toggleDateLock, batchLockDates.' },
  { version: '0.64.0', date: '2026-02-25', summary: '⚡ TIMELINE ENGINE — LIG-010 COMPLETE: Row Inactivation Toggle (inactivate clears predecessors/dates/resource, re-activate restores templated values, hide-inactive filter, audit timestamp). LIG-015 COMPLETE: UQ Trigger Milestone Auto-Calculation (TriggerMilestoneBanner, 5 milestone presets + custom, working-day date engine with holiday skip, topology sort, override tracking, recalculate button, weekend-adjusted badge). LIG-016 COMPLETE: Critical Path & Dependency Graph (CriticalPathPanel SVG graph, topology-sorted layout, forward+backward pass zero-float calculation, circular dependency detection, dep graph button, Critical Only filter, zoom controls, jump-to-task, detail panel with successor derivation). New: date-calculation-engine.ts. New demo plan: OncoPrime BLA (UQ 8-Week template). Store: toggleTaskInactive, setTriggerMilestone, recalculateDates, overrideTaskDate, clearDateOverride, refreshCriticalPath.' },
  { version: '0.63.0', date: '2026-02-25', summary: '📋 TEMPLATE REGISTRY + CCP + HOLIDAY CALENDAR — LIG-006 COMPLETE: TemplateRegistryScreen with 6 demo templates (3 built-in, 1 imported, 1 connected-source, 1 retired), self-service import wizard (CSV/JSON/Connected Source, 3-step), retire/re-activate with active-plan guard, changelog modal, versioned metadata. LIG-009 COMPLETE: CCP Reference on M3 rows in GSP — isCCPReferenced + ccpReference fields on GSPTask, teal CCP badge in plan grid, amber Review CCP badge for classification changes, ccpReviewPending alert banner in plan summary. LIG-019 COMPLETE: Holiday Calendar Integration — holiday-calendar-data.ts with US/EU/JP 2026 calendars (28 entries), 7 holiday rows seeded in demoGSP, locked row rendering (colored bands, Locked badge), excluded from all progress/task count calculations, HolidayCalendarPanel in Template Registry for admin config with toggle/expand/info. Sidebar: Template Registry nav entry.' },
  { version: '0.62.0', date: '2026-02-25', summary: '📋 SUBMISSION PLANNING COMPLETE — 4 PRDs closed. LIG-008 COMPLETE: CPI Detail Panel (14 items), Shared/Unique/All filter, Jump-to-task row highlight. LIG-005 COMPLETE: Template Picker Step 2 in wizard (8-week/16-week/40-market). LIG-003 COMPLETE: Add Submission modal with delta task insertion + CPI row linking. LIG-004 COMPLETE: Provisioning Error Banner with error detail, Retry, dismiss, spinner state. OncoTarget GSP demo seed with provisioning-failed.' },
  { version: '0.61.0', date: '2026-02-25', summary: '🗺️ SUBMISSION PLANNING — Live GSP/LSP screen. LIG-002 COMPLETE: provisioned plan grid, lead market core tasks, satellite delta tasks, collapsible CTD sections, progress bars. LIG-007 COMPLETE: per-row Submission(s) + Country/Market columns (filterable), Lead Market flag column. LIG-008 partial: CPI Reference toggle column. Plan Selector: GSP (Ligrastinib NDA/MAA/J-NDA) + LSP (CardioShield NDA). Terminology: ISP → GSP/LSP throughout. Sidebar: Submission Planning nav entry.' },
  { version: '0.60.0', date: '2026-02-25', summary: '⚡ Reg Intelligence + Cascade Deep-Links — (A) Alert Rules Config: slide-in panel with 5 default rules (keyword/compound/source/category match types), auto-triage assignment, email notify toggle, enable/disable per-rule, add/delete; (B) Bulk Triage: checkbox on every alert card, amber BulkTriageBar with count + batch status dropdown, clears selection after apply; (C) Alert→Submission Impact Linking: Link button in detail panel opens modal with active submissions list + impact note textarea, existing links shown per-alert (Layers badge on cards), stored on alert.submissionLinks; (D) CPI Dedup View: Step4Review collapsible table — lead market column + satellite columns, 10 CPI rows each with green checkmark per satellite, delta row shows region-specific tasks, inherited CPI note footer; (E) CascadeBanner Step Deep-Links: module icon in StepRow is clickable (navigates to targetModule), spawned steps get "Open →" button navigating to owning module.' },
  { version: '0.59.0', date: '2026-02-25', summary: '🗺️ Submission Planning + Reg Intelligence — (A) LIG-001/002: GlobalSubmissionPlanWizard Step 2 redesigned with Lead Market designation and CPI (Common Package Items) model — first region auto-set as lead, "Set as Lead" button per region, lead market shows 10 CPI items shared to satellites, each satellite shows its region-specific delta task list (EU 5 tasks, JP 4, CN 4, etc.). (B) LIG-030/031: New RegulatoryIntelligenceScreen — 8 demo alerts from FDA/EMA/PMDA/ICH/MHRA, per-alert relevance scoring bar (0–100) per compound, triage status picker, detail panel with compound impact analysis and rationale, stat row, compound exposure bar chart, source filter, full-text search. (C) CascadeBanner: MODULE_ICONS expanded, RunningChainStrip Action Center deep-link. (D) SourceViewerDrawer: live store sources. (E) Admin: Manage modal wired.' },
  { version: '0.58.0', date: '2026-02-25', summary: '🐛 Bug Fixes + Demo Polish — (A) eCTD Builder: fixed folder tree scroll (overflow-hidden removed from module cards); fixed document viewer black pane (bg-slate-900 → bg-surface-elevated, all dark-theme colors replaced). (B) View Source: SourceViewerDrawer component wired — clicking View Source opens a slide-in panel with document sections and excerpts. (C) User Management: replaced Admin placeholder with functional invite flow — invite by email, role assignment, copy link, user directory table. (D) CascadeConfirmationModal: confirm/dismiss UI for preview-state chains, surfaced from HomeScreen. (E) NonclinicalScreen: Finalize Study button on GLP study cards fires real cascade. (F) Signal→Label cascade: LabelingScreen now surfaces label-change tasks from confirmed safety signals. (G) CompoundLifecycleStrip: CTMS enrollment count and HAQ count bound to live store data. (H) Demo Reset: PresenterDashboard reset button clears all cascade chains and cross-module events with confirm step.' },
  { version: '0.57.0', date: '2026-02-25', summary: '⚡ LIVE CASCADE WIRING — (A) Added glp-study-finalized CascadeTriggerType with 6-step chain definition (Safety surveillance, IND flag, CSR authoring, HAQ prediction, Protocol amendment, eCTD Module 4). (B) Added demo-glp-cascade CrossModuleEventType with 6 CascadeActions (all automated:true). (C) GuidedScenarioOverlay: added fireTrigger hook — when frc-4 fires demo-glp-cascade, cascade engine fires simultaneously, surfacing the R&D Connected chain on HomeScreen in real time. (D) PresenterDashboard: full scenario list with Flagship badge and valueProposition display for Full Regulatory Cascade. (E) CascadeBanner: expanded MODULE_ICONS/COLORS to cover regulatory, ctms, haq, nonclinical.' },
  { version: '0.56.0', date: '2026-02-25', summary: '🎯 INVESTOR DEMO POLISH — (A) Model update: all API routes updated from claude-sonnet-4-5-20250929 to claude-sonnet-4-6 (14 files). (B) Full Regulatory Cascade scenario: built out from 1 placeholder step to 14 comprehensive steps across 7 modules — the flagship investor demo. (C) Compound Lifecycle Pipeline: new CompoundLifecycleStrip component on HomeScreen showing 3 demo programs (Nexavant/LIG-2847, CardioShield/LIG-3901, OncoTarget/LIG-5104) with clickable stage chips navigating to live modules.' },
  { version: '0.55.0', date: '2026-02-25', summary: '🏁 SCREENHEADER FINAL SWEEP + CAPABILITYGATE COMPLETE — (A) ScreenHeader: 5 more screens (CommitmentsManager, EnhancedDocumentVersioning, PlatformAnalyticsDashboard, RiskCAPAIntegrationDashboard, AIConsistencyEngineDashboard). Final adoption: 53 top-level screens + 4 ScreenLayout screens = comprehensive coverage. Excluded: 11 intentional (embedded sub-components, modals, ScreenLayout, portals). (B) CapabilityGate final sweep: 4 more actions gated (CTMSDeviationMonitoring Schedule Visit/author, RegistrationsTracker Add Registration/author, RegulatoryCalendarScreen Add Event/author, ECTDIntelligenceScreen Import eCTD/author). Total: 26 gated screens.' },
  { version: '0.54.0', date: '2026-02-25', summary: '🏗️ SCREENHEADER BATCH 5 + CAPABILITYGATE SWEEP — (A) ScreenHeader adoption: 13 screens (AIDocumentGenerationScreen, AdPromoScreen, AuditTrailDashboard, CTMSDeviationMonitoring, ContinuousSubmissionDashboard, ECTDIntelligenceScreen, PortfolioStrategyView, RegistrationsTracker, RegulatoryCalendarScreen, StudyDesignScreen, SubmissionCommandCenter, SubmissionsHubScreen, UnifiedCMCManufacturingScreen). Total adoption now 54/~74 screens (73%). (B) CapabilityGate wired to 6 screens: BiostatsScreen (AI Analysis/author), MasterDataScreen (New Entity/author), SubmissionCommandCenter (New Submission/submit), SubmissionsHubScreen (New Submission/submit), AdPromoScreen (Submit Material/submit). Total gated screens: 17.' },
  { version: '0.53.0', date: '2026-02-25', summary: '🏗️ SCREENHEADER BATCH 4 + CAPABILITYGATE WIRING — (A) ScreenHeader adoption: 14 screens updated (ActivityScreen, BiostatsScreen, CommercialScreen, DevelopmentScreen, ELNScreen, GCPScreen, GLPScreen, GMPScreen, InspectorReadinessScreen, LabelingScreen, ManufacturingScreen, MasterDataScreen, MedicalAffairsScreen, RegStrategyScreen). Total adoption now 41/74 screens. (B) CapabilityGate wired to 6 screens: QualityScreen (New Record/author), GCPScreen (Report Deviation/author), GLPScreen (New Study/author), GMPScreen (Report Deviation/author), ELNScreen (New Experiment/author), ResearchScreen (Add Alert/author).' },
  { version: '0.52.0', date: '2026-02-25', summary: '🎯 SCREENHEADER BATCH 3: MODULE LANDING PAGES — Full title/subtitle/icon adoption across 14 screens. (1) Bare ScreenHeader upgrades (had moduleId only, now full): HAQScreen (MessageSquare icon), AuthoringScreen (FileText), CTMSScreen (FlaskConical), QMSScreen (Shield), TMFScreen (Archive), SafetyScreen (Activity). (2) Full ScreenHeader adoption (no ScreenHeader previously): AdminScreen (Settings icon, header before sidebar layout), CMCScreen (FlaskConical, before tab toolbar), QualityScreen (Shield + New Record action), NonclinicalScreen (Microscope + tab view switcher in actions), PSMFScreen (Shield + compliance badge + tabs in children), RDArchivesScreen (Archive + export/sync actions + tabs in children), ResearchScreen (FlaskRound, before tab toolbar), ActionCenterScreen (dynamic greeting title + RefreshCw action, stat pills kept in StatusSummary below). 27 of ~74 screens now use ScreenHeader.' },
  { version: '0.51.0', date: '2026-02-25', summary: '🎯 SCREENHEADER ADOPTION (Batch 2) — ScreenHeader wired into 5 demo-path screens: PortfolioScreen (title+subtitle+search+actions with LayoutDashboard icon), HomeScreen (persona greeting+avatar preserved as icon slot, pain point callout in children), PublishingCenterScreen (title+stats actions, breadcrumb+tabs separated below), ECTDWorkspaceScreen (title+stats+New Sequence action, breadcrumb+view toggle separated below), ProgramDetailScreen (back button as icon, program code title, status badge, metadata in children). No CapabilityGate needed — visual consistency only. Respects embedded mode for Publishing/eCTD screens. 13 of ~74 screens now use ScreenHeader.' },
  { version: '0.50.0', date: '2026-02-25', summary: '🎯 SCREENHEADER ADOPTION (Batch 1) — ScreenHeader + ScreenBody wired into all 8 CapabilityGate screens (Authoring, HAQ, CTMS, Safety, QMS, Submissions, DocumentControl, TMF). Replaces ad-hoc h1+subtitle+ViewOnlyBanner patterns with standardized ScreenHeader component. Fixed QMS ViewOnlyBanner bug (was embedded inside useMemo computation). Moved ViewOnlyBanner from sub-components to top-level ScreenHeader in CTMS, TMF, Safety. Deeper CapabilityGate wiring: QMS New Deviation/New CAPA/New Audit buttons gated, Submissions New Sequence gated, Safety New Case gated.' },
  { version: '0.49.0', date: '2026-02-25', summary: '📱 PWA ENABLEMENT — (1) Web App Manifest: standalone display, Ligature branding, icon declarations for all platforms. (2) App icons: generated 192/512 regular + maskable PNGs, apple-touch-icon, favicons — Ligature "L" lettermark with blue/teal accent. (3) Service worker (pwa-sw.js): network-first strategy for all requests (regulatory data must never be stale), static asset caching for offline fallback, version-keyed caches, offline.html shell. (4) PWARegistration component: replaces ServiceWorkerKiller — production-only SW registration, version-aware localStorage cleanup, update detection with toast notification, 30-min update polling. (5) layout.tsx: manifest link, theme-color meta, apple-web-app capable, favicon chain, removed inline SW-kill script. (6) Offline page: branded fallback with retry button.' },
  { version: '0.48.0', date: '2026-02-25', summary: '🎨 UI CONSISTENCY PASS — (1) ScreenHeader + ScreenBody reusable components: standardized title/subtitle/actions/badge/icon pattern with ViewOnlyBanner integration. (2) Design system v0.48.0: color tokens (accent + semantic status), screen header presets (tw.screenTitle/screenSubtitle/screenHeaderRow), status badge presets (tw.statusSuccess through tw.statusNeutral), card hover preset. (3) globals.css polish: semantic status color CSS variables + utility classes, text selection color, thin scrollbar variant, smooth scroll, card-hover lift effect, subtle-pulse animation, consistent input focus transitions. (4) Status color mapping guide: approved→success, in-review→warning, rejected→error, in-progress→info, queued→pending, draft→neutral.' },
  { version: '0.47.1', date: '2026-02-24', summary: '🔌 CAPABILITY GATE WIRING — CapabilityGate + ViewOnlyBanner integrated into 8 key module screens: Authoring (New Document, Finalize, Send to eCTD, Approve with Signature), HAQ (Package Responses), Safety (banner), CTMS (New Study), QMS (Submit for Review, Approve), Submissions (banner), Document Control (New Document), TMF (Upload Document). All primary create/approve/submit actions now respect capability entitlements.' },
  { version: '0.47.0', date: '2026-02-24', summary: '🏗️ CAPABILITY-LEVEL ENTITLEMENTS — Enterprise-flexible module access. (1) ModuleCapability type: view, author, approve, configure, submit — hierarchical action classes. (2) TierCapabilityMap: per-bundle-tier capability assignments enabling "all modules, authoring only" and "full regulatory, view-only clinical". (3) ORG_TIER_CAPABILITIES: org tier→bundle→capabilities mapping (professional gets view-only clinical/quality/cmc). (4) Updated DEMO_PRESETS: new "Authoring Only" preset (all modules, view+author only), "Professional" now shows view-only tiers. (5) useFeatureFlags v2: hasCapability(), getModuleCaps(), isViewOnly(), isRestricted() selectors + capabilityMap state + persist migration v1→v2. (6) CapabilityGate component: inline action gating with disabled overlay + upgrade prompt. ViewOnlyBanner for restricted modules. useCapabilityGate hook. (7) Sidebar: three access states (locked/view-only/restricted/full) with eye icon and access label badges. (8) ModuleLockedScreen: capability breakdown showing what upgrading unlocks. (9) TierSwitcher: capability summary per preset.' },
  { version: '0.46.1', date: '2026-02-24', summary: '🛡️ HYGIENE + VERIFICATION — (1) ESLint barrel file prevention: no-restricted-syntax rule blocks export-all and re-export patterns in index.ts files, preventing TDZ regression. (2) Fixed BUILD_ID mismatch (was v0.45.2, now v0.46.1). (3) Verified persona system: 6 personas, PersonaSwitcher in Header, persona-aware HomeScreen all confirmed working. (4) Verified citation monitor: 12 regulatory citations, CitationMonitorPanel in AdminScreen, API routes + cron confirmed wired.' },
  { version: '0.45.2', date: '2026-02-24', summary: '🔧 STABILITY + PERSONAS + COMMAND CENTER + CITATION MONITOR — (1) SW rebuilt: _next/ excluded, navigate passthrough. (2) ServiceWorkerKiller in layout. (3) webpack concatenateModules:false. (4) Persona system: 6 buyers, PersonaSwitcher, usePersonaStore. (5) HomeScreen rebuilt: persona greeting, 2×3 KPI hero tiles (22 KPIs), reordered pulse, cascade step highlights, pain point callout. (6) Citation Monitor: 12 regulatory citations (ICH E6/E2B/Q10/M4/E8/Q1A, FDA 21CFR11/312/314, EMA GVP VI/IX, DIA TMF RM), SHA-256 fingerprint change detection API, Vercel cron (Monday 08:00 UTC), useCitationMonitor hook, CitationMonitorPanel in Admin, vercel.json. (7) SafetySyncStatus duplicate fixed.' },
  { version: '0.45.1', date: '2026-02-24', summary: '🚀 FEATURE FLAGS — Modular rollout foundation. (1) module-registry.ts: ModuleId→BundleTier mapping for all 50+ modules across 7 commercial tiers (Core, Regulatory, Clinical, Safety, Quality, CMC, Research). (2) useFeatureFlags Zustand store: tier-based gating with isModuleEnabled(), org tier presets, demo preset switcher. (3) ModuleLockedScreen: polished locked-state component with module description, tier badge, feature highlights, and unlock CTA. (4) Sidebar gating: locked modules show dimmed with lock icon, still navigable (page.tsx renders locked screen). (5) page.tsx gating: dynamic import guard renders ModuleLockedScreen for gated modules. (6) TierSwitcher: admin-only dropdown in sidebar footer to switch between tier presets during demos (Core Only → Seed Demo → Enterprise).' },
  { version: '0.45.0', date: '2026-02-24', summary: '🐛 TDZ BUG RESOLUTION + SERVICE WORKER FIX — (1) Resolved CTMS TDZ crash (Cannot access eP before initialization) caused by circular barrel imports in @/components/ui + ectd-document-templates.ts cycles. Replaced all 97 barrel imports with direct file imports, converted ui/index.ts to export * format. (2) Disabled webpack concatenateModules + added modularizeImports as belt-and-suspenders. (3) Service worker cache lock: rebuilt public/service-worker.js with new CACHE_VERSION, removed /_next/ chunk caching, added ServiceWorkerKiller component to root layout. (4) Auth persistence bug resolved as consequence of SW fix.' },
  { version: '0.44.54', date: '2026-02-23', summary: '🐛 FIX: Stuck syncing — 5s timeout on apiFetch in useSafetyPersistence so offline/no-DB state resolves to offline rather than spinning. SafetySyncStatus auto-collapses to Local mode after 6s. Login bypass is expected behaviour (valid JWT cookie from previous session).' },
  { version: '0.44.53', date: '2026-02-23', summary: '✨ HOME SCREEN — R&D Command Center entry experience. (1) R&D Connected section: live cascade chains displayed as trigger → step flow with module colour coding and progress bar. Empty state prompts Safety navigation. (2) Platform Pulse: 8-module metric strip, urgent modules highlighted in their accent colour, all clickable. (3) Needs Attention: cross-module urgent items (overdue docs + critical signals + due-soon cases) with urgency bars. New home ModuleId, sidebar Home button pinned above My Tasks, default module set to home.' },
  { version: '0.44.52', date: '2026-02-23', summary: '✨ TMF DEEP DIVE — Module sweep complete. (1) Cascade strip (cyan ⚡): protocol-approved triggers show Seed Structure CTA targeting studies tab. (2) Auto-select: lowest completeness-score study selected on load, eliminating empty right panel. (3) Compact header: h1+icon+subtitle block removed, single descriptor line. All 8 modules now have consistent cascade strips, hero items, and compact headers.' },
  { version: '0.44.51', date: '2026-02-23', summary: '✨ AUTHORING DEEP DIVE — (1) Dual cascade strips: amber ⚡ for safety-signal-confirmed labeling tasks (Open Labeling CTA), purple ⚡ for db-lock-completed CSR tasks (Start CSR CTA) — two chains, two colours, one consistent pattern. (2) Hero document card: most overdue/due-soon doc featured with border-l urgency styling and solid Open Editor button. (3) Compact header: Welcome banner removed, single descriptor line.' },
  { version: '0.44.50', date: '2026-02-23', summary: '✨ CTMS DEEP DIVE — (1) Auto-select: highest-risk/enrolling study is now selected on data load, eliminating empty right panel. (2) Cascade strip (teal ⚡): shows protocol-approval triggered CTMS study creation tasks with Create Study CTA. (3) Compact header: h1+icon block removed, single descriptor line, saves ~60px.' },
  { version: '0.44.49', date: '2026-02-23', summary: '✨ SAFETY DEEP DIVE — (1) Dynamic hero signal card: replaced hardcoded hepatotoxicity banner with data-driven card (critical confirmed → critical active → high confirmed → any open). Click navigates to signal detail. (2) Compact header: h1+subtitle removed. (3) Urgency-sorted signal list: critical/confirmed signals float to top with red tinted card styling, confirmed badge, bold text; high signals amber tinted; PRR+class effect metadata inline.' },
  { version: '0.44.48', date: '2026-02-23', summary: '✨ SUBMISSIONS DEEP DIVE — (1) Fixed wrong default: ind-portfolio (no render) → overview (app card grid). (2) Cascade strip in overview: safety-triggered amendment flags render as red ⚡ strips with Review Impact CTA. (3) Hero blocked/delayed submission card with solid action button surfaced above the app grid. (4) Compact header: h1+subtitle removed, single descriptor line.' },
  { version: '0.44.47', date: '2026-02-23', summary: '✨ QMS DEEP DIVE — (1) QMSCascadeStrip: shows cascade tasks targeting qms module (CAPA investigations from safety signals), blue themed, Open Investigation CTA. (2) DashboardView: cascade strip + alerts now appear BEFORE KPI grid (urgency order). (3) Hero CAPA card: most critical/overdue open CAPA featured with solid action button above KPI stats.' },
  { version: '0.44.46', date: '2026-02-23', summary: '⚡ CASCADE ENGINE BITES 6+7+8 — (6) CTMS DB Lock chain wired: CTMSScreen fires trigger on first db-lock tab visit, spawns CSR authoring + submission milestone steps. (7) Sidebar cascade badge: amber ⚡N indicator appears next to My Tasks count when chains are pending, reads useCascadeCount. (8) My Tasks UX redesign: action button moved to top-right (always visible), title dominant/larger for featured first card, description + causality condensed below title at left indent, bottom row shows module tag + due date only — cleaner scan path.' },
  { version: '0.44.46', date: '2026-02-23', summary: '✨ HAQ DEEP DIVE — (1) cascadeEngineTypes: added HAQ target step to safety-signal-confirmed chain (Review Open HAQ Responses for Signal Impact, medium-risk). (2) HAQCascadeStrip component: reads cascade chains targeting haq module, renders amber ⚡ strip above stats with Review CTA and dismiss. (3) HAQ dashboard hero card: surfaces single most urgent open question (overdue first, then critical, then soonest due) with solid red/amber action button. (4) Compact header: removed 80px h1+subtitle block, replaced with single descriptor line.' },
] as const;
